# Decision Log
<!-- doc-status: active -->

Record durable methodological and architectural decisions here. Keep entries short enough to scan.

## Template

### YYYY-MM-DD: Decision title

- **Context:** What changed or what result forced a choice?
- **Decision:** What did we decide?
- **Reason:** Why is this the right tradeoff now?

## 2026-09-18: Accept resolution_concordance_1626_1630 as final; stop pursuing missing_htr completeness

- **Context:** `resolution_concordance_1626_1630` (Steps 1-5 in PLAN.md)
  reached 70.7% `resolved_auto`, 28.6% `missing_htr`, 0.6% `nihil_actum`.
  Step 1's diagnostic showed 49.7% of the whole ledger has no HTR session for
  its inventory anywhere within +/-14 days -- a structural coverage ceiling,
  not a matching-quality gap. The Step 5 window-widen follow-up already
  closed the addressable part of that gap (0/475 entity-overlap signal
  before the fix, 125/475 after); the remainder showed no further signal at
  any tested window.
- **Decision:** Accept the current concordance output as final. Do not
  pursue further completeness work on the remaining `missing_htr` rows.
  Attempted a bounded, one-time pagexml spot-check first (inventory 4861,
  100% `missing_htr`; inventory 4562's `1629-05-12` "Crèvecoeur" row) but
  found it is not actually cheap: `docs/DATA.md`'s claim that pagexml lives
  at `/Volumes/2tb disk/data/` is stale -- that directory only holds
  `ead_metadata-extended.tsv` and `emigratie`. The real archive is
  `/Volumes/2tb disk/datasets/republic/source/1.01.02-pagexml.tgz`
  (88.5 GB, unextracted, not registered in `data_manifest.toml`); the
  companion `sessions_json-2026-02-27.tar.gz` (10 GB, also unextracted) sits
  alongside it. Listing/extracting specific inventories from an 88.5 GB
  tarball is real heavy compute, not a quick `ls`/grep, so the spot-check
  itself is deferred rather than run as part of closing this track.
- **Reason:** The wide-window test found essentially zero entity-overlap
  signal at +/-2..+/-7 days on the unresolved rows (475/475 at 0.0 before
  the fix), strong evidence the ceiling reflects absent source coverage
  rather than a fixable matching/extraction defect -- and the archive's
  actual size/state (unextracted, unregistered, un-indexed) makes verifying
  that against pagexml directly a nontrivial project of its own, not a
  sanity check. Chasing full completeness here has demonstrated near-zero
  marginal return; effort is better spent on the separate, still-open HTR
  under-segmentation problem (S4 Segmentation Transfer, 81.1% of
  session-days affected) or the NER training track. If pagexml verification
  is wanted later, it needs its own scoped step: register the archive (or a
  targeted extraction of it) in `data_manifest.toml` first, per project
  convention, rather than reaching into `/Volumes/2tb disk/...` directly.
  `docs/DATA.md`'s pagexml location is also now known-stale and should be
  corrected separately.

## 2026-09-18: Do not start line-level segmentation yet; verify ground-truth coverage first

- **Context:** docs/S4_ITERATION_REVIEW.md flagged line-level cut points (per SEGMENTATION_TRANSFER.md section 9) as the anticipated next unit for the 12 gold days where the paragraph axis is coarser than K_e, citing 'supervision already available upstream' (NeuralLineClassifier, htr_classified_lines.csv, res_start).
- **Decision:** Do not build the line-level DP/classifier pipeline this session. First map the 1,294 checked, target-inventory rows in the marijn-variant CSV to calendar dates and check overlap with the 12 gold days -- a cheap join, not the full pipeline.
- **Reason:** Verified directly: the canonical htr_classified_lines.csv (14,324 rows) has zero rows in target inventories 3185-3189 -- period-skewed to inventory 3156 and ~176 others, same zero-overlap failure res_start already had. The marijn-variant file does cover the right inventories (1,294 checked rows, 121 para_start / 123 para_end) but at spot-check density (2-9 sampled pages per inventory out of hundreds), unmapped to dates, and unlikely to include these specific 12 days. Building the full DP/classifier pipeline (plus resolving the score_typed_overlap vs align_session scoring-convention mismatch, plus new (enriched_id, line_id) entity lookups) on an unverified data assumption risks exactly the disproportionate-effort-for-uncertain-gain outcome flagged when this was scoped -- the res_start precedent shows this corpus's upstream ground truth does not reliably cover the 1626-1630/3185-3189 window.

## 2026-09-18: Diagnose S4 boundary-F1 stagnation as a paragraph-axis granularity ceiling, not a scorer defect

- **Context:** PLAN.md's 2026-09-18 S4 session flagged an open question: boundary micro F1 stayed exactly 0.576 (tol0) / 0.727 (tol2) even though the per-boundary snap-collision fix moved 20 more boundaries from raw interpolation to phrase-snapped positions (6/32 -> 26/32) on the 5 multi-boundary predicted gold days. A per-boundary gold-vs-predicted comparison (reusing existing s4_paragraph_axis_predictions.jsonl and boundary_gold_paragraph_axis, no rerun) found the cause: gold's own boundary annotations repeat the identical paragraph_stream_index for multiple distinct K_e-1 cut points whenever the paragraph axis is coarser than the resolution count (e.g. 1626-02-28: 6 boundary slots collapse onto 1 distinct axis position, axis_count=8 for k_e=7). Across all 17 eligible gold days with any boundary, 16 have at least one such collision; 41/179 gold boundary slots (22.9%) corpus-wide share a position with another slot in the same day.
- **Decision:** Treat boundary_f1_tolerance0/tolerance2 as having hit a structural ceiling from paragraph-level axis granularity, not a defect in interpolation/snapping. Stop trying to move these two metrics via better interpolation or phrase-snapping; that effort is spent. Any further F1 gain requires finer-than-paragraph segmentation, which is the already-scoped line-level-segmentation track (state.json status: blocked, pending htr_classified_lines.csv date-coverage verification). S4's other metrics (predicted-day coverage, corpus baseline coverage, paragraph-axis entity coverage) are unaffected and remain open.
- **Reason:** compute_boundary_prf awards at most one hyp match per distinct ref value, and predicted boundary sequences are always strictly increasing (never repeat a position), so duplicate-valued gold slots impose a hard recall ceiling regardless of predictor quality: max reachable TP = count of distinct ref values, not len(ref). Verified on the 5 multi-boundary predicted days: tol0 TP (19) already reaches 79% of that ceiling (24 distinct values across 34 ref slots) and tol2 TP (24) reaches exactly 100% of it -- the snap-collision fix improved individual boundary position accuracy, but the metric was already saturated against the axis-imposed ceiling both before and after, which is why F1 did not move.

## 2026-09-18: Correct line-level-segmentation blocker: page-to-date join needs new tooling, not a cheap check

- **Context:** docs/SEGMENTATION_TRANSFER.md section 9 and the 2026-09-18 blocked decision described the next step as 'a cheap join' -- map the marijn-variant htr_classified_lines_marijn.csv page_ids (e.g. NL-HaNA_1.01.02_3156_0313) to calendar dates and check overlap with the 12 gold days. Checked what that join would actually require.
- **Decision:** The 'cheap join' framing is wrong and is corrected here. No dataset in data_manifest.toml maps (inventory, scan/page) to date for inventories 3185-3189. session_index_all.parquet and session_date_status_1626_1630 both key on (inventory_id, session_num/enriched_date) only -- no scan/page field. Getting a page_id like ...-page-624 to a calendar date requires opening individual session JSON records inside the unextracted sessions_json-2026-02-27.tar.gz (10 GB, same archive flagged as heavy/deferred in the 2026-09-18 resolution-concordance decision) to read each session's page range, then joining that to session_date_status_1626_1630. This is new tooling over an unextracted 10 GB archive, not a quick lookup. Task stays blocked; do not attempt the join as a quick check in a future session -- scope it as its own step (register a targeted extraction in data_manifest.toml first) or drop it.
- **Reason:** Per ITERATION_POLICY.md's anti-pattern list, a design doc's 'this check is cheap' claim needs verifying against the real files before scoping work around it -- same lesson as the res_start and htr_classified_lines.csv zero-overlap findings already in this doc. Verified directly this session: session_index_all and the session-date ledger are both session-level, not scan/page-level, so the join needs a new page-range extraction step, which is exactly the kind of heavy/unbounded archive work this project's workflow defers to an explicit, scoped step rather than running inline.

## 2026-09-18: Explain the boundary_gold_paragraph_axis data gap for 1626-05-17 and 1627-04-11

- **Context:** docs/S4_ITERATION_REVIEW.md flagged 2 of the 21 eligible gold days (1626-05-17, 1627-04-11) as having zero paragraph-axis records at all -- 'not investigated further' at the time -- distinct from the 12 structurally-abstaining days (axis coarser than K_e).
- **Decision:** Not a pipeline bug. resolutions_flat has zero rows for both calendar dates, so build_boundary_gold_paragraph_axis.py correctly emits zero axis records -- there is nothing to attach. Checked output/boundary_gold_sample.json directly: both days have k_e=1, k_f=0, flat_ids=[] -- the enriched record exists (162617mei.xml#0 / 162711ap.xml#0) but no flat/HTR resolution was ever matched to it. This is the same missing_htr structural ceiling already accepted as final for resolution_concordance_1626_1630 (2026-09-18 decision, 49.7% of the ledger has no HTR within +/-14 days), showing up here as a paragraph-axis gap instead. Since k_e=1 for both, they are trivial zero-boundary days with no segmentation signal to lose -- same category as the two k_e==1 zero-anchor days (1628-01-01, 1630-03-03) already fixed this iteration. No further action needed; do not re-investigate this as a new bug.
- **Reason:** Root-caused with data already on disk (resolutions_flat, boundary_gold_sample.json) -- no new tooling or heavy compute needed. Confirms the missing_htr ceiling is the same underlying cause surfacing in a second track, which is useful corroboration but not a new problem to solve, consistent with that track being closed as final.

## 2026-09-19: Add a proposal step to svz.py review; iterative tasks surface to strategy on completion

- **Context:** docs/APPROACH_OVERVIEW.md traced why whole branches (align_short_resolutions.py, the corpus-wide fuzzy scan) became orphans: work that is not registered is not measured, work that is not measured is not iterated. The 2026-09-19 session was the worked example -- svz.py review ran at session start and correctly flagged S4's boundary F1 as stagnant, but because it reported status without proposing a next step, the session still spent itself on the one branch that already had an evaluation harness, plus a long local-LLM infrastructure detour that advanced no track.
- **Decision:** Three changes. (1) Registered the orphans as real tracks: short-resolution-alignment (todo), fuzzy-surface-form-scan (inprogress), plus interior-cut-evaluation-harness (todo) -- a missing piece, not an orphan script, without which interior-cut work is structurally invisible to the tracked S4 metrics. (2) svz.py review now ends with a 'Proposed next step' section naming one recommended track with a reason and a goal, ranked by leverage (count of downstream tracks) then cheapness, never by momentum; unmeasurable tracks get a ranking bonus because they cannot be judged at all. Tasks gained optional next_action/goal/cost/blocks fields (svz.py update --next-action/--goal/--cost/--blocks), with next_action falling back to parsing 'Next action:' from existing notes. doctor validates blocks references and cost values. (3) Adopted control flow: finishing or terminating a subtask returns to strategy and RECOMPUTES the recommendation from the new state rather than resuming the previous queue -- because a completed subtask can change which track is highest-leverage. Termination must write back a metric and an outcome; descents get a budget whose breach is itself a trigger to surface early. Revert is the default, overridable by the user.
- **Reason:** The failure was never missing information -- the status board was accurate and was read. It was that reporting status leaves the prioritising to whoever is already deepest in a branch, and that person is the least able to notice they should be elsewhere. A recommendation with a stated reason and goal can be argued with; a status board can only be scrolled past. Ranking by leverage makes the argument explicit: the overlap-builder audit now surfaces first because it is upstream of 2 open tracks and would make 2 closed ones suspect if it fails, at cheap cost -- an ordering no amount of momentum would have produced.

## 2026-09-19: excel-overlap-builder-audit: literal-substring bug confirmed

- **Context:** place/org/per_overlap_1626_1630.xlsx builders were suspected of the same id-as-name/literal-substring bug class fixed in build_alignment_new.py by entity-resolution-fix. build_windowed_overlap.py already contains an unused --compare-legacy rebuild path (variant-lookup + tag_text fallback) for place/org; no equivalent existed for PER.
- **Decision:** Confirmed yes for all three. Place: build_windowed_overlap.py --window-days 0 --compare-legacy shows legacy canonical-only matching (9,428 rows) is a strict subset of the variant+tag_text-aware rebuild (10,693 rows, legacy_only=0, rebuilt_only=1,265, +13.4%). Org: legacy 1,363 rows vs rebuilt 1,418 (legacy_only=0, rebuilt_only=55, +4.0%). Per: build_per_overlap.py has no fuzzy/variant pass at all (exact substring + exact-word surname-token fallback only, verified by reading the source) -- directional cross-check against entity_surface_matches_1626_1630 finds 1,123 fuzzy-confirmed PER pairs across 991 resolutions entirely absent from per_overlap_1626_1630.xlsx (different candidate universe than per_overlap, so not a clean rate, but consistent with the same bug class). Also found in passing: build_windowed_overlap.py's institution-name lookup uses a separate unregistered instelling_info.json path rather than entity-resolution-fix's instituten_lookup_cleaned -- checked and the two are value-identical (0 diffs, same 56 rows), so no live discrepancy today, but it is a second untracked path to the same fact and should be collapsed onto one source.
- **Reason:** legacy_only=0 in both rebuild comparisons proves the fix is strictly additive (no regressions), so this is a safe, cheap swap rather than a re-litigation risk. Quantifying it (rather than leaving it as a suspicion) unblocks a decision on whether to adopt the rebuilt tables as canonical.

## 2026-09-19: adopt-windowed-overlap-rebuild: canonical place/org overlap swapped

- **Context:** build_windowed_overlap.py --window-days 0 rebuild was verified strictly-additive on the confirmed key set (legacy_only=0, +1265 place / +55 org rows) 2026-09-19; versioned outputs (place_overlap_same_day_1626_1630.xlsx, org_overlap_same_day_1626_1630.xlsx) already existed on disk, no rebuild needed.
- **Decision:** Swapped canonical data/derived/place_overlap_1626_1630.xlsx and org_overlap_1626_1630.xlsx to the rebuilt content (10693/1418 rows, dedup on volgnr+paragraph_id+name). Legacy content backed up to data/derived/legacy_backup/*.pre_rebuild_2026-09-19.xlsx. Manifest descriptions updated; data_io.check passes for both paths. Downstream consumers (build_alignment_new.py, s4_paragraph_axis_baseline.py, s4_corpus_paragraph_predictions.py) NOT yet re-run -- that is heavy compute left for the user. PER variant-aware rebuild (build_per_overlap.py has no fuzzy pass) still not scoped or built.
- **Reason:** Cheap, zero-regression swap unblocks the leverage this track was picked for (upstream of S4 and short-resolution-alignment); re-running downstream consumers and scoping the PER rebuild are separate, costlier next actions rather than part of this same decision.

## 2026-09-20: S6: reframe segmentation as multi-channel anchor chaining at character coordinates

- **Context:** User proposed (1) reformulating the problem as DNA-style sequence alignment, (2) integrating ALL anchor contributions into one alignment comparison, (3) making sessions an anchor rather than a cutoff. Checked against docs/SEGMENTATION_TRANSFER.md section 6: NW-over-entity-sequences was already the design, and section 6.3 already specified that cut points land at CHARACTER positions and that the paragraph-vs-line granularity question 'dissolves'. The implementation drifted: s4_paragraph_axis_baseline.py:155-156 emits paragraph_stream_index as the output coordinate, with char_offset only as a phrase-snap byproduct (0 when no hit).
- **Decision:** Registered track s6-anchor-chain-alignment (todo, multi-session) with guide docs/steps/STEP_S6_anchor_chain_alignment.md. Measured the coordinate claim on all 50 gold days: 109/352 cut slots (31.0%, across 25 days) share a paragraph index; distinct reachable positions 280 -> 320 under (paragraph_stream_index, char_offset), hard recall ceiling 79.5% -> 90.9%, with all 32 residual unseparated slots having char_offset=null (annotation gap, not a coordinate limit). This REOPENS boundary_f1_tolerance0/tolerance2, closed 2026-09-18, on a new axis -- legitimately, because character coordinates are the 'finer-than-paragraph granularity' that closure required, and unlike the line-level track they need no new ground truth and no sessions_json extraction. Anchors are weighted per provenance GROUP (A tagger entity layer, B dictionary/fuzzy surface, C formulaic text, D structural/temporal), never flat, because group A's three channels share one tagger. Algorithm is colinear anchor chaining, not global NW and not an off-the-shelf bioinformatics library.
- **Reason:** The ceiling that closed those metrics was an implementation artifact of the output coordinate, not a property of the data -- and the character-level ground truth needed to prove it was already sitting in output/boundary_gold_sample.json (332/373 boundary records carry char_offset). Sequencing S6a (character-coordinate harness) first is non-negotiable: the interior-cut branch already demonstrated that work invisible to the tracked metrics does not accumulate.

## 2026-09-20: Correction: the paragraph-granularity ceiling is rigorous only at tolerance 0

- **Context:** The 2026-09-18 entry closing boundary_f1_tolerance0/tolerance2 stated that compute_boundary_prf 'can award at most one hyp match per distinct ref value', giving 'max reachable TP = count of distinct ref values', and read the 5-day subset result 'tol2 TP (24) reaches exactly 100% of it' as evidence the metric was saturated. The S6 guide written 2026-09-20 initially repeated that framing. Probed it directly against evaluation_harness.compute_boundary_prf.
- **Decision:** The claim holds at tolerance 0 only. At tol=0 a hypothesis must equal a reference exactly, and since predictions are strictly increasing, at most one can claim each distinct ref value -- a hard bound. At tol>0 it is not a property of the metric: against ref=[7,7,7], a CLUSTERED hypothesis [5,6,7] scores TP=3 at tolerance 2, while a spread [3,7,11] scores TP=1. The bound appeared to hold at tol2 only because the interpolate-then-snap predictor emits one cut per paragraph by construction and so cannot cluster. Therefore tol2 was NOT provably saturated on 2026-09-18; there was headroom the ceiling framing concealed. Corrected in docs/steps/STEP_S6_anchor_chain_alignment.md section 1(a); the tol0 figures (280/352 = 79.5% paragraph coords, 320/352 = 90.9% char coords) are unaffected and remain rigorous.
- **Reason:** The overstated version would have mis-set S6's expectations twice over: it understates how much tol2 headroom the current predictor is leaving on the table, and it credits the metric with a limitation that actually belongs to the predictor family. Both readings still argue for character coordinates -- placing several cuts inside one paragraph is exactly what they enable -- so the S6 direction is unchanged, but the stated reason had to be made accurate before the track is executed against it.

## 2026-09-20: S6a: character-axis ceiling is 313/352 (0.889), not 320/352 (0.909)

- **Context:** STEP_S6_anchor_chain_alignment.md section 1 states that moving to (paragraph_stream_index, char_offset) raises the tolerance-0 hard recall bound on the 50-day gold set from 280/352 (79.5%) to 320/352 (90.9%), and S6a's exit criterion is to record that ceiling as a metric.
- **Decision:** Record the ceiling as 313/352 = 0.889. Of 352 gold cut slots, 39 carry char_offset=null; those 39 fall into 7 distinct (date, paragraph_index) groups. The 320 figure counts those 7 groups as reachable positions alongside 313 genuinely distinct character positions (313 + 7 = 320). A slot with no offset has no character coordinate, so it is unreachable on this axis by construction.
- **Reason:** Verified by strict composition in scripts/s6a_char_axis_evaluation.py: all 313 offset-bearing cuts compose to 313 distinct positions (no collisions remain), 0 cuts are off-axis, and 0 cuts fall on axis-less days. A paragraph-start fallback for null offsets would recover exactly 320, but those 7 positions would each absorb several folded slots, so it buys reachability by reintroducing the folding the axis exists to remove. The character axis still beats paragraph coordinates by a real margin (0.795 -> 0.889); only the size of the gain changes, +9.4 points rather than +11.4.

## 2026-09-20: S6 Step 1: the placement model binds, not anchor supply

- **Context:** STEP_S6 section 2.3 proposes colinear anchor chaining to replace S4's entity-only NW. Chaining improves which anchors are trusted; it does not change what happens between chosen anchors, nor the coordinate the model emits. An oracle-anchor run is therefore an upper bound on what any anchor-selection improvement, chaining included, can reach.
- **Decision:** Do not build S6c's chaining DP on the current placement model. Handed a PERFECT gold-derived anchor set, interpolate_positions + snap_boundaries predicts only 5 of 19 scoreable gold days (coverage 0.263): 9 days abstain folded_anchors_non_monotone and 5 axis_shorter_than_k_e. On the 5 it can predict, micro F1 is 0.727 / 0.788 / 0.788 at tol 0 / 50 / 150 chars. Proceed instead with S6b (anchor harvest, useful under any algorithm) and a count-constrained segmentation DP that places exactly K_e-1 cuts at character granularity.
- **Reason:** 74% of days are unusable with perfect information, so anchor selection is not the binding constraint. Two structural causes, both untouched by a better search: interpolate_positions requires anchors strictly increasing in both coordinates, which genuinely-folded gold cuts violate; and the model emits (paragraph index, snap offset), so the 33 of 177 mid_paragraph gold cuts (18.6%) are largely unreachable at tight tolerance -- which is why oracle F1 caps at 0.788 rather than near 1.0. Measured by scripts/s6_oracle_anchor_diagnostic.py (18 tests) on the S6a character axis. CAVEAT: the all-density (5 days) and endpoints-density (11 days) rows cover different day sets -- endpoints covers more precisely because thinning to two anchors dodges the non-monotone check -- so the 0.788 vs 0.645 gap is not a density effect.

## 2026-09-20: S6b constraint baseline: gap counts flatter the problem; 19% of gaps hold 75% of the work

- **Context:** SEGMENTATION_TRANSFER.md section 7 argues that with ~1 tier-1 anchor every 2.3 rows, unanchored gaps are short (typically 1-2 resolutions) and therefore 'the residual problem is small and locally constrained'. Measured directly for the first time over a contiguous window (1626-01-01..1626-06-30) by scripts/s6b_known_point_ledger.py, which chains typed known points (session start/end sentinels, tier-1 anchors, gold boundaries) at character coordinates and counts the resolutions that must fall in each gap.
- **Decision:** The section 7 claim holds by gap count and fails by resolution count. Of 495 gaps: 263 are determined (0 free resolutions), 139 nearly determined (1-2), 93 open (>2). That is 81.2% determined-or-nearly BY GAP -- but those gaps hold only 179 of 723 unplaced resolutions (24.8%), while the 93 open gaps hold 544 (75.2%), spanning 397k characters and running as long as 21 resolutions. Report the resolution-weighted share alongside any gap-count share; the gap-count figure alone is misleading.
- **Reason:** A gap of size 0 contributes a row to the distribution but zero unplaced resolutions, so gap-count statistics are dominated by already-solved intervals. Only 389 of 1218 resolutions (31.9%) are pinned by a known point at all (366 tier-1 anchors, 41 gold boundaries across the 4 gold days in the window). Separately, 75 of the 181 days in the window have enriched resolutions but no paragraph axis at all, consistent with the accepted missing_htr ceiling. Effort should concentrate on the 93 long gaps rather than on the already-constrained majority.

## 2026-09-21: adopt-windowed-overlap-rebuild: canonical PER overlap swapped

- **Context:** The 2026-09-19 entry adopted the variant/tag_text-aware rebuild for place/org but left PER unscoped (`build_per_overlap.py` had no fuzzy pass). `build_per_overlap.py` was since extended to reuse `build_windowed_overlap.py`'s canonical/variant/tag_text framework plus a person-specific surname-token fallback pass, and run as `--window-days 0 --compare-legacy` against the real corpus.
- **Decision:** Swapped canonical `data/derived/per_overlap_1626_1630.xlsx` to the rebuilt content (7,959 rows: 2,678 canonical + 2,572 variant + 2,033 tag_text + 676 surname-token matches, 4,038/4,106 paragraph_ids resolved). On the `volgnr`+`paragraph_id`+`name` key, `legacy_only=0` (strict superset), `shared=7,544`, `rebuilt_only=415`. Legacy content backed up to `data/derived/legacy_backup/per_overlap_1626_1630.pre_rebuild_2026-09-21.xlsx`. Manifest description updated; `data_io.check` passes. Downstream consumers (`build_alignment_new.py`, `s4_paragraph_axis_baseline.py`, `s4_corpus_paragraph_predictions.py`) NOT yet re-run against the now-fully-swapped place/org/per overlap trio — that remains heavy compute left for the user.
- **Reason:** Same zero-regression bar as the 2026-09-19 place/org swap: every legacy-matched key is retained, plus 415 net-new rows from the variant/tag_text/surname-token passes the literal-substring builder missed. Closes the "build the equivalent fuzzy pass for PER" half of the `adopt-windowed-overlap-rebuild` track; only the downstream re-run half remains open.

## 2026-09-21: S6b anchor harvest: anchor supply is not the corpus-wide bottleneck

- **Context:** STEP_S6_anchor_chain_alignment.md's S6b row asks for an anchor harvest across all four provenance groups (A tagger entity layer, B dictionary/fuzzy surface, C formulaic text, D structural/temporal) and a measurement of "how far it closes the 815 insufficient_entity_anchors abstentions." Built `scripts/s6b_anchor_harvest.py` (9/9 tests) and ran it corpus-wide over 1626-1630: 247,871 anchor rows over 1,240 days-with-axis (group A 5,332 tier1 anchors/993 days; group B 18,406 `entity_surface_matches` + 189,764 `fuzzy_surface_scan` anchors/up to 1,238 days; group C 14,890 + 16,999 phrase-hit anchors/~1,231-1,233 days; group D 2,480 session sentinels).
- **Decision:** Of the 815 corpus-wide `insufficient_entity_anchors` abstentions, only 59 (7.2%) had zero group-A anchors at all; the other 756 (92.8%) already had at least one group-A anchor and abstained anyway, because `interpolate_positions` rejects them for monotonicity or axis-length reasons, not anchor scarcity. Of the 59 zero-anchor days, 57 (96.6%) gain a non-A anchor once groups B/C are harvested, but that is only 57/815 = 7.0% of all abstentions -- despite group B/C anchor volume being enormous relative to group A (`fuzzy_surface_scan` alone touches 162,307 distinct character positions across 1,238 of 1,240 days). Therefore: do not build S6c as a chaining step whose deliverable is resolving more group B/C anchors into `interpolate_positions`-compatible enriched-index pins -- that would leave 93% of abstentions untouched. Redirect S6c to a count-constrained segmentation DP that works directly inside each gap from `s6b_known_point_ledger.py`'s chain (947 open corpus-wide gaps hold 70.7% of unplaced resolutions per the 2026-09-20 constraint baseline), using B/C raw positions as soft in-gap placement evidence rather than as pre-resolved anchors.
- **Reason:** This corroborates the S6 Step 1 oracle-anchor diagnostic (2026-09-20) at full corpus scale rather than just the 19 scoreable gold days: handing the model a perfect anchor set there still only unlocked 5/19 days, because the placement model itself -- not anchor supply -- was shown to bind. The anchor-harvest run tests the same claim from the supply side (more anchors, still the old placement model) and gets the same answer (~93% unaffected), which is strong enough agreement across two independent measurements to redirect engineering effort rather than build the chaining step the original step guide proposed.

## 2026-09-21: Caveat on the S6b anchor-supply finding: measured on stale alignment/prediction inputs

- **Context:** The same-day "S6b anchor harvest: anchor supply is not the corpus-wide bottleneck" decision above rests on `alignment_1626_1630.parquet` (source of every group-A `tier1_entity_nw` anchor) and `s4_corpus_paragraph_predictions.jsonl` (source of the 815-day `insufficient_entity_anchors` list). Checked file mtimes while following up on `svz.py review`'s repeated recommendation of `adopt-windowed-overlap-rebuild`: `alignment_1626_1630.parquet` was last built 2026-08-28 and `s4_corpus_paragraph_predictions.jsonl` 2026-08-31 -- both before the place/org overlap rebuild (2026-09-19) and the PER overlap rebuild (2026-09-21) landed. Neither file has been regenerated against the now-fully-swapped overlap tables (+1,265 place / +55 org / +415 PER rows).
- **Decision:** Treat the 815 / 59 / 57 figures as **not yet final**. Re-run `build_alignment_new.py` and `s4_corpus_paragraph_predictions.py` against the swapped overlap tables (closing `adopt-windowed-overlap-rebuild`'s own open downstream-re-run action), then re-run `scripts/s6b_anchor_harvest.py` on the refreshed inputs and check whether the 92.8%-of-abstentions-already-have-a-group-A-anchor split still holds before treating it as load-bearing for the S6c redesign.
- **Reason:** The placement-model ceiling (`interpolate_positions`'s monotonicity/axis-length rejection) is a property of the algorithm, not the anchor count, so it is likely to survive a refresh -- but "likely" is not "measured," and this project's own discipline (docs/ITERATION_POLICY.md) is to verify rather than assume a claim still holds against current data. Cheap to check: both re-runs were already scoped as `adopt-windowed-overlap-rebuild`'s next action before this caveat was found.

## 2026-09-21: S6b anchor-supply finding re-confirmed on refreshed inputs -- caveat resolved

- **Context:** The 2026-09-21 caveat flagged that the 815/59/57 anchor-supply split was measured on alignment_1626_1630.parquet (built 2026-08-28) and s4_corpus_paragraph_predictions.jsonl (built 2026-08-31), both predating the place/org overlap rebuild (2026-09-19) and PER overlap rebuild (2026-09-21). Re-ran build_alignment_new.py (11:43) then scripts.s4_corpus_paragraph_predictions (11:44) against the swapped overlap tables, then scripts.s6b_anchor_harvest (11:57-12:28, parallelized+checkpointed, see scripts/s6b_anchor_harvest.py) on the refreshed inputs, full 1626-1630 corpus, 1240 days, 247914 anchor rows.
- **Decision:** The split holds and is slightly more lopsided than before: 777 insufficient_entity_anchors abstentions (down from 815), of which only 19 (2.4%, down from 59/7.2%) have zero group-A anchors, and 18 of those gain a non-A anchor from groups B/C (2.3% of all abstentions, down from 57/7.0%). 758/777 = 97.6% of abstentions (up from 92.8%) already had a group-A anchor and abstained anyway. Caveat resolved: do not build S6c as a chaining step that resolves more anchors for the existing interpolate_positions. The binding constraint is the placement model's monotonicity/axis-length rejection, confirmed on both the S6 Step 1 oracle diagnostic and now this anchor-supply measurement on fresh data.
- **Reason:** This project's discipline (docs/ITERATION_POLICY.md) is to verify a load-bearing claim against current data rather than assume a stale measurement still holds; the refresh was cheap (build_alignment_new.py + s4_corpus_paragraph_predictions + a newly-parallelized s6b_anchor_harvest, ~35 min total wall time) relative to the cost of building S6c on a wrong premise.

## 2026-09-21: S6c target metric rescoped: paragraph-level coverage, not character-exact tolerance

- **Context:** Investigated whether any downstream consumer of segmentation-transfer boundaries actually needs character-exact cut points, before committing to S6c's design. Findings: s4_resolution_concordance.py (the one real downstream assembly step, accepted 2026-09-18) treats paragraph attribution as explicitly optional best-effort, not a required status input (docs/CANDIDATE_SCORING_AND_CONCORDANCE.md:327-338). build_alignment_new.py, the live entity-matching pipeline, operates entirely on paragraph_id -> resolution_id and never reads character offsets. NER training pairs (build_loc_training_pairs.py / build_per_training_pairs.py) take their character offsets from the separate LOC-/PER-annotations.json layer, unrelated to segmentation-transfer output. resolution_concordance_1626_1630 currently has zero downstream readers. The char-axis (S6/S6a/S6b) push itself was motivated by defeating a self-imposed plateau on boundary_f1_tolerance0 (docs/DECISIONS.md 2026-09-20), not by a named consumer's requirement.
- **Decision:** S6c should target coverage and paragraph-level (lumped) accuracy, not tol0/tol50 character-exact placement. This also sidesteps the risk flagged earlier: S6c's originally-scoped in-gap placement (using B/C anchors as soft evidence to place a precise character cut) would likely have inherited s4_bundled_split_poc.py's already-measured weak precision (0-15% recall at tol50). Scoped to paragraph-level placement instead, S6c's job narrows to fixing interpolate_positions's over-eager abstention (only 5/19 days predicted even with oracle-perfect anchors, per the S6 Step 1 diagnostic) while keeping today's paragraph-level placement quality (0.828 lumped) -- a much easier, better-evidenced target.
- **Reason:** User confirmed the suspicion that full resolution-boundary precision is not needed; verified against actual code/doc citations rather than assumed. Keeps S6c scoped to the part of the problem (abstention/coverage) the project has strong positive evidence it can fix, instead of the part (sub-paragraph character placement) it has strong negative evidence against.

## 2026-09-21: adopt-windowed-overlap-rebuild: downstream re-run checked, headline tier stats barely move -- closing the track

- **Context:** The track's own goal (state.json) was 'canonical overlap tables include the variant/fuzzy recovery, and downstream tier/confidence stats reflect it (or a measured reason not to adopt)'. The place/org swap (2026-09-19) and PER swap (2026-09-21) were both done, and build_alignment_new.py / s4_corpus_paragraph_predictions.py were re-run against them on 2026-09-21 (11:43-11:44) as part of closing the S6b anchor-supply caveat -- but nobody had checked build_alignment_new.py's own headline output (confidence_tier distribution) against the pre-swap numbers documented in PLAN.md's 'Tiered Milestone Alignment' section.
- **Decision:** Loaded the current alignment_1626_1630.parquet (13342 rows, same total as before the swap) and compared confidence_tier counts to the pre-swap figures already in PLAN.md: tier1_anchor 5725->5730 (42.9%->42.9%), tier2_merged_page+tier2_interpolated+tier2_adjacent 4734->4708 (35.5%->35.3%), tier3_head_template+tier3_tail 2883->2904 (21.6%->21.8%). Largest single-tier delta is 26 rows, 0.19 percentage points. Mark adopt-windowed-overlap-rebuild done: both halves (place/org rebuild, PER rebuild) were adopted, the downstream re-run happened, and the measured reason for the near-flat headline stats is now on record.
- **Reason:** This corroborates the S6b anchor-supply finding from the same refresh (insufficient_entity_anchors abstentions 815->777) rather than contradicting it: the +1265/+55/+415 recovered rows are a small fraction of the ~13.3k alignment pairs and the ~18.7k entity_surface_matches corpus, so they nudge tier composition without materially reshaping it. The overlap-table swap was still worth doing (zero-regression, net-new true positives per the 2026-09-19/09-21 swap entries) -- it just isn't the lever that moves S4/S6 headline metrics, consistent with S6b's conclusion that anchor supply is not the binding constraint there either.

## 2026-09-21: S6c: count-constrained segmentation DP built and wired in

- **Context:** S6c was rescoped 2026-09-21 to fix interpolate_positions's over-eager abstention (only 5/19 oracle-anchor days predicted) rather than resolve more anchors for it. Two failure modes named by the oracle diagnostic: folded_anchors_non_monotone (9/19) and axis_shorter_than_k_e (5/19), both cases lumped paragraph-level scoring already tolerates.
- **Decision:** Built scripts/s6c_gap_segmentation.py (segment_gap/segment_day, 11 tests) and swapped it in for interpolate_positions in both scripts/s4_paragraph_axis_baseline.py and scripts/s4_corpus_paragraph_predictions.py. It never abstains: out-of-order anchors are clipped forward into a non-decreasing backbone anchored at session start/end sentinels, and gaps narrower than their resolution count get repeated paragraph assignments instead of aborting the day. Group-C phrase hits feed it as soft in-gap evidence at gold-day scale; group B and corpus-scale phrase evidence are deferred (would need a ~30min parallel FuzzyPhraseSearcher pass like s6b_anchor_harvest.py). interpolate_positions itself is untouched -- s6_oracle_anchor_diagnostic.py still needs the unmodified model.
- **Reason:** Re-ran the 50-gold-day pipeline (evaluate_s4_paragraph_axis.py, s6a_char_axis_evaluation.py): predicted-day coverage 9/21 -> 19/21 (eligible) and 7/19 -> 19/19 (scoreable, S6a's denominator) -- full coverage of every day with an axis. Lumped paragraph-level F1 held essentially flat (0.828 -> 0.826 at tol50/150), and strict character F1 was roughly flat at tol50 (0.500 -> 0.503) despite predicting on nearly 3x as many days, including the harder ones the old model used to refuse. This is the coverage win S6c's rescoped target asked for, achieved without a corpus-wide run.

## 2026-09-21: S6c corpus-wide re-run: insufficient_entity_anchors eliminated

- **Context:** Previous session validated S6c's count-constrained segmentation DP only on the 50 gold days (coverage 9/21 -> 19/21). The corpus-wide question left open: does the same abstention-free behavior hold over the full 1626-1630 window, where the last measured insufficient_entity_anchors count was 777 (of ~1594 enriched-dated days, 535 structurally missing_htr).
- **Decision:** User ran uv run python -m scripts.s4_corpus_paragraph_predictions (no code changes since the prior session). Result: 1059 predicted / 535 abstained, and the abstained set is now missing_htr only -- insufficient_entity_anchors is exactly 0 (was 777). 1059 = 282 (pre-existing predictions) + 777 (every previously-abstained day now predicts), an exact match confirming the mechanism, not a coincidence.
- **Reason:** This is a decisive corpus-scale confirmation of the gold-day result: 100% of days with any paragraph axis now receive a prediction. Quality caveat quantified directly from the output (no gold labels exist corpus-wide, so this substitutes for an F1 check): of 1059 predicted days, 302 are clean (no repeated paragraph index), 516 have a minor 2-3-way collapse, 114 have 4-6, and 79 (7.5%) have 7+ resolutions collapsed onto one paragraph (max 24) -- these are low-localization coverage wins, not segmentation, an expected and disclosed consequence of allowing repeats rather than a bug. S6c's rescoped exit criterion (coverage + gold-day lumped F1, not char precision) is met; group-B evidence and corpus-scale phrase-hit wiring remain deferred, scoped as the next increment only if the severe-collapse share needs improving.

## 2026-09-21: S6d pivoted: wire corpus predictor to resolution_concordance_1626_1630 instead of building session-as-anchor chaining from scratch

- **Context:** STEP_S6_anchor_chain_alignment.md scoped S6d as 'drop the per-day partition; run per inventory with session starts as group-D anchors' to recover the 535 missing_htr days and 7 gold cross-day-shift days. Before building that, found a separate, more mature, already-'accepted final' track (S4a-g: session_date_status_1626_1630 -> s4_day_status_resolution -> resolution_concordance_1626_1630) that already resolves session/day mapping per enriched resolution, including confident entity-overlap candidate scoring over ambiguous -1/+1/? ledger rows -- exactly the orphan pattern STEP_S6 section 1b warned about: multiple resolution mechanisms built and not talking to each other. s4_corpus_paragraph_predictions.py (the thing S6c just fixed) was grouping paragraphs by raw calendar date only and never consulted it.
- **Decision:** Wired scripts/s4_corpus_paragraph_predictions.py to fall back to resolution_concordance_1626_1630's resolved_session_id (day_status == resolved_auto) when a date has no same-calendar-day axis, instead of building new session-as-anchor alignment/chaining logic. Two new pure functions (session_of, axis_for_date, 4 new tests) plus a resolved_sessions() loader. Did NOT auto-select ledger -1/+1 candidates directly (rejected as a separate option): docs/SESSION_DATE_MAPPING_REVIEW.md already decided those need human review with a mandatory note, and 95% of unique -1/+1 candidates turned out to also be their true neighbor day's own T/E match, confirming that needs a human looking at the actual text, not a blanket policy.
- **Reason:** Re-ran uv run python -m scripts.s4_corpus_paragraph_predictions (no other changes): predicted 1059 -> 1140, missing_htr abstentions 535 -> 454, exactly 81 days recovered for free from work already done and already accepted by a separate track. Full test suite (354 passed) and data_io.check are clean. This is a data-source wiring fix, not new segmentation logic -- consistent with the 'replace, don't accrete' guardrail. Remaining gaps are out of scope for this session: the 365 -1/+1 candidates need the pending human-review workflow (docs/SESSION_DATE_MAPPING_REVIEW.md), and the 7 gold cross-day-shift days (trailing contaminated text on a day that DOES have its own axis) are a different failure mode this wiring does not touch.

## 2026-09-21: S4f review surfaced resolutions_flat session-numbering drift -- bigger than the -1/+1 queue itself

- **Context:** Took the S6d handoff's option (a): scoped build_session_date_mapping_review_ui.py to just the -1/+1 rows (--status filter, 365 rows) and excluded 64 nihil-actum rows (drop_nihil_actum_rows, already auto-resolved ahead of human review by s4_day_status_resolution.py's precedence), leaving 301. User hand-reviewed ~10 and exported decisions (data/import/s4_session_date_mapping_decisions.json). 4/9 approvals confirmed a real mid-session-merge signal -- a "President de Heer X, Present de <weekday> den <date>" session-start formula appearing partway through one HTR-parsed session block, meaning two real sessions got concatenated under one session id. A new lenient diagnostic (scripts/s4_session_start_scan.py, prefix-stem regex on presid/presen) reproduced this at scale: 210 candidate mid-text starts across 167/1511 sessions (11.1%). But 3/9 no-match decisions cited true dates 2-3 days outside the ledger's +/-1-day candidate window, found via app.goetgevonden.nl's per-resolution URNs. Investigating those dates led to pulling raw sessions_json_source JSON directly (new warm-tier dataset s4_session_date_region_scan, 51677 text_regions across all 1690 sessions in the ledger's 6 covered inventories, 179 more than resolutions_flat holds any row for at all).
- **Decision:** The app.goetgevonden.nl dates turned out not to be independent ground truth -- they matched resolutions_flat's own per-session date exactly wherever checked. The real finding came from matching a 150-char content fingerprint from each resolutions_flat session against its own inventory's raw sessions in sessions_json_source: 1128/1511 (75%) matched a unique raw session, 0 ambiguous, 383 unmatched. resolutions_flat's session numbering has drifted from the current archive, confirmed directly (e.g. resolutions_flat's session-3185-num-25-resolution-1 text matches raw session-3185-num-26 verbatim; num-26's matches raw num-27). Drift shape varies by inventory: 3185/3186/3189 accumulate a monotonic +1-at-a-time staircase (reaching +9/+13/+8 by year end), 3187 is mild (flat at 0 for ~240 sessions, then +1, +2), 3188 is bidirectional (starts at -2, crosses to +8), and 4562 oscillates between 0/+1/+2 dozens of times rather than accumulating -- a qualitatively different, unexplained case. Every step changes by exactly +/-1, consistent with individual sessions being inserted or removed one at a time as the archive was re-segmented since resolutions_flat was built.
- **Reason:** A real share of -1/+1 (and likely A/X/N) ledger rows reflect this numbering drift rather than genuine nearby-day ambiguity -- the offered candidate session id is simply mislabeled relative to its actual content, so no amount of human review of the current queue framing fixes that. Fixing the ledger itself needs a per-inventory realignment against sessions_json_source, out of scope for continuing the -1/+1 review as currently framed. Chose not to keep manually reviewing the 301-row queue once this was clear (~10 rows were enough to surface and validate the pattern). Next step scoped as two new Group-D channels in scripts/s6b_anchor_harvest.py per STEP_S6 section 2.2, not yet built: session_day_find (direct date-phrase hits from raw text_regions, self-sufficient across all 1690 sessions, catches both false-negatives and false-positives already found in the upstream nlc_classifier's own "date" text_region_class tag) and session_date_verified (the content-fingerprint correspondence needed specifically to know what resolutions_flat currently mislabels a piece of content as). Open design question before either is a quick bolt-on: s6b's char_position axis is built from resolutions_flat's own (possibly drifted) text, so the new channels must anchor on content or archival provenance (scan_id/page_id/line_id), not the session-number label. The 1128 confirmed offset matches are only in scratch (/tmp/scope_results.json), not yet persisted to a manifest dataset.

## 2026-09-21: S6b: session-fingerprint match persisted as a registered dataset

- **Context:** PLAN.md's 2026-09-21 S4f session left the content-fingerprint correspondence between resolutions_flat session ids and raw sessions_json_source sessions (needed for STEP_S6 section 2.2's session_date_verified anchor channel) only in unregistered scratch (/tmp/scope_results.json, 1128/1511 matched via an undocumented method), explicitly flagged as needing regeneration/proper persistence before the channel work starts.
- **Decision:** Built scripts/s6b_session_fingerprint_match.py (9 tests) and registered s6b_session_fingerprint_match in data_manifest.toml. Method: 150-char normalized fingerprint of each resolutions_flat session's resolution text, matched by substring containment (not exact-prefix equality) against same-inventory raw sessions' concatenated 'para'-class region text from s4_session_date_region_scan. Exact-prefix matching found only 838/1511 (55.5%); substring containment finds 1059/1511 (70.1%) with 0 ambiguous matches, closer to and more principled than the scratch figure. Channel construction (session_day_find, session_date_verified) itself is not built this session -- this closes only the 'persist the fingerprint data' prerequisite.
- **Reason:** resolutions_flat's own paragraph splitting does not align 1:1 with raw para region boundaries (e.g. an attendance-like line mistagged 'para' gets spliced out of the flat text), so a flat session's real content can start partway into a raw session's concatenated text -- exact-prefix equality systematically undercounts genuine matches for this reason, not because the correspondence doesn't exist. data_io.check and the full test suite (363 passed) are clean.

## 2026-09-21: S6b: session_day_find channel built with FuzzyPhraseSearcher, not the stem regex -- and validated before wiring in

- **Context:** Built the first of the two Group-D channels the S4f/fingerprint-match sessions scoped: `session_day_find`, the internal "President de Heer X, Present de <weekday> den <date>" session-start formula found past a day's own opening (signalling a merged HTR session block). The obvious approach was to port `s4_session_start_scan.py`'s stem regex (`pr[ae]{1,2}sid\w*` / `pr[ae]{1,2}sent\w*`) as-is; queried instead whether `FuzzyPhraseSearcher` (already used for every other phrase channel in this file) would do better, since a literal regex can't absorb HTR character noise inside a correctly-spelled stem.
- **Decision:** Built `phrase_hits`/`session_day_find_rows` in `scripts/s6b_anchor_harvest.py` using `FuzzyPhraseSearcher` over two phrase lists grounded in the corpus itself (frequency scan over `paragraph_axis_1626_1630`): all 8 forms covering ~98% of "presid*" occurrences, and a **narrowed** present-family list (`present`, `presentibus`, `praesentibus`, `presentie`, `presente`) -- checked directly what co-occurs with "presid*" in a 60-char window (237/830 occurrences do) and found the corpus-wide-frequent `presentatie`/`presenterende`/`presenteren`/`presenteert` (generic "presentation"/attendance usage) appear there **zero times**; including them would only add noise. Wired as a new Group-D channel (weight 4.0, between tier1's 3.0 and the session-boundary sentinel's 5.0).
- **Reason:** Two bugs found and fixed before trusting any number. (1) `FuzzyPhraseSearcher`'s `ignorecase` defaults to `False` -- against real corpus text (sentence-initial "Preside et Presentibus...") this undercounted the regex baseline (210 hits/167 sessions on the same session-text universe) by 4x (54/45) until set to `True`; this also affects the existing `s2_anchor_phrases`/`s4_opening_phrase_candidates` channels' shared `PHRASE_SEARCH_CONFIG`, left unchanged here since their metrics predate this finding -- separate follow-up, not folded in silently. (2) At the existing 0.85 threshold, ignorecase-fixed, hit count jumped to 576/365 -- inspecting sampled snippets showed most were false positives: single short words like "present" (7 chars) fuzzy-match unrelated nearby substrings often enough that a generic "de heer President heeft geproponeerde..." mention got spuriously paired with noise and counted as a formula hit. Raising the threshold to 0.95 (still `ignorecase: True`) dropped the count to 154/130 with 25/25 sampled hits genuine (spot-checked twice, different random samples) -- lower recall than the regex baseline (unvalidated for precision) but real. **Scope note:** unlike STEP_S6's original wording ("self-sufficient: works on all 1,690 raw sessions"), this channel operates over `paragraph_axis_1626_1630` like the rest of the harvest, i.e. still resolutions_flat-derived, not raw `sessions_json_source` text_regions -- it does not yet reach the 179 sessions with zero resolutions_flat rows. `session_date_verified` (the other planned Group-D channel) is not built. 17/17 new+existing tests pass, full suite (367 passed) and `data_io.check` clean; not yet run corpus-wide (main()'s ~32-minute parallelized pass), only validated against the same session-text universe `s4_session_start_scan.py` used.

## 2026-09-21: session_day_find corpus-wide run -- smallest channel by volume, supply-gain proxy now degenerate

- **Context:** User ran the corpus-wide harvest (`uv run python -m scripts.s6b_anchor_harvest`, full 1626-1630 window) with `session_day_find` wired in, no other code changes.
- **Decision:** No code change; recording the corpus-scale reading. `session_day_find`: 203 anchors / 162 of 1,240 days-with-axis (13.1%), 203 distinct positions -- smallest of all six non-sentinel channels by anchor count (group A `tier1_entity_nw` alone has 5,375 anchors/995 days; group B's `fuzzy_surface_scan` has 189,764). `insufficient_entity_anchors` supply-gain proxy printed 0/0/0.
- **Reason:** The 0/0/0 proxy is degenerate, not a finding -- S6c (2026-09-21, above) already eliminated the `insufficient_entity_anchors` abstention category corpus-wide (777 → 0), so there is no abstained-day set left for the proxy to check gains against; it would read 0/0/0 regardless of what any new channel found. `session_day_find`'s real signal is its low density (162/1,240 = 13.1% of days touched) and validated precision (154/130 on the same session-text universe, 25/25 sampled genuine) -- a sparse, high-confidence anchor, not a coverage lever. Consistent with expectations: internal session-merge formulas are rare events (11.1% of raw sessions per `s4_session_start_scan.py`), not a per-day-typical signal like tier1 entities or phrase openings.

## 2026-09-21: S6b: session_day_find channel built with FuzzyPhraseSearcher, validated before wiring in

- **Context:** Built the first of two planned Group-D channels (STEP_S6 sec 2.2): an internal president/present session-start formula found mid-axis, signalling a merged HTR session block. Chose FuzzyPhraseSearcher over porting s4_session_start_scan.py's stem regex, since the project's own guardrails favor reusing the existing phrase-search tool over a bespoke pattern.
- **Decision:** Wired phrase_hits/session_day_find_rows in scripts/s6b_anchor_harvest.py using FuzzyPhraseSearcher with ignorecase=True and threshold 0.95 (its own SESSION_START_PHRASE_SEARCH_CONFIG, not the shared PHRASE_SEARCH_CONFIG), and a present-phrase list narrowed to forms empirically confirmed to co-occur with presid* in a 60-char window.
- **Reason:** Two bugs found before trusting any count: (1) FuzzyPhraseSearcher's ignorecase defaults False, undercounting the s4_session_start_scan.py regex baseline (210/167) by 4x until fixed; (2) at the default 0.85 threshold, short words like 'present' fuzzy-match unrelated substrings, producing mostly false positives (spot-checked). At 0.95, 25/25 sampled hits were genuine; final count 154/130, lower recall than the unvalidated regex baseline but real. Scope caveat: still resolutions_flat-derived via paragraph_axis_1626_1630, not raw sessions_json_source, so it does not reach the 179 sessions with zero resolutions_flat rows. 17/17 tests in the file, full suite 367 passed, data_io.check clean.

## 2026-09-21: S6c: corpus-scale group-C phrase-hit wiring tested on gold days, measured negative, not wired in

- **Context:** Scoped wiring s4_opening_phrase_candidates evidence into s4_corpus_paragraph_predictions.py's segment_day call (the gap S6c's own docstring named as deferred, not the full ~32-min corpus-wide run). Refactored scripts/s4_paragraph_axis_baseline.py's phrase_hits(axis, phrases) to phrase_hits(axis, searcher) so a searcher can be built once and reused, matching s6b_anchor_harvest.py's pattern -- verified behavior-preserving by diffing s4_paragraph_axis_predictions.jsonl byte-for-byte before/after (identical). Before wiring the corpus predictor's main() to build a searcher and running the expensive full corpus pass, scored the change cheaply on just the 19/21 scoreable gold days using the corpus predictor's own predict()/grouped_enriched()/axis_for_date() codepath (not the separate gold-day baseline pipeline), reusing evaluate_s4_paragraph_axis.py's evaluate_segmentation_session/summarize_segmentation_evaluations harness.
- **Decision:** Did not wire phrase-hit evidence into the corpus predictor. Reverted the predict()/main() changes back to the unmodified segment_day(...) call with no position_scores, keeping only the phrase_hits refactor in s4_paragraph_axis_baseline.py (a pure, verified-identical efficiency change, independently useful).
- **Reason:** Measured net negative on the primary metric: tol0 F1 0.644 (no phrase evidence) -> 0.622 (with phrase evidence, via segment_day's position_scores bonus), same 19/21 coverage both ways. tol1/tol2 saw only marginal gains (+0.006 each) and mean Pk/WindowDiff got worse (0.285 -> 0.292). Also tested adding snap_boundaries (the gold-day pipeline's second phrase-evidence stage, which the corpus predictor never had) on top of the DP bonus -- produced IDENTICAL numbers, confirming this is a real result and not an artifact of a half-ported mechanism. Net effect: the phrase bonus sometimes pulls segment_gap's chosen position away from the correct one when several resolutions share a gap, trading exact hits for near-misses. Full test suite and data_io.check remain clean; no corpus-wide run was needed to reach this answer, saving the ~32-minute job for a result that would not have justified it.

## 2026-09-21: Demote tolerance metrics in favor of concordance and collapse counts

- **Context:** Character-level tolerance metrics (tol0/tol150) punish OCR noise/spacing and mask multi-resolution paragraph collapsing.
- **Decision:** Demote tol0/tol150 character metrics; adopt paragraph-level concordance and max-collapse count per session as primary drivers.
- **Reason:** Tolerance windows treat discrete paragraphs as continuous space and penalize semantically valid formulaic phrase snaps.

## 2026-09-22: Backfill acceptance-criteria decision (separated definition, ceiling, targets table)

- **Context:** PLAN.md's canonical-goal block (PLAN.md:17) cites 'docs/DECISIONS.md, 2026-09-22' as the source of the separated/ceiling/targets definition, but no such entry existed -- the last entry was 2026-09-21. Discovered while consolidating docs/STATE.md's metrics into docs/METRICS.md.
- **Decision:** Record here, as stated in PLAN.md: a resolution is separated iff its start paragraph is shared with no other resolution on its date and its extent is <=3 paragraphs. Paragraph-level ceiling: 11,644/19,120 = 60.9%. Targets against that ceiling: Global primary >=50% (>=5,822), Global stretch >=75% (>=8,733), Global spans >=6 qualifying spans totalling >=180 days, Local: any inventory-year reaching >=50% of its own ceiling. Baseline: 1,961 = 16.8% of ceiling.
- **Reason:** These numbers were manually derived and cited as a decision that was never logged. No script in the repo reproduces the literal figures (11644, 19120, 1961) -- flagged as an open gap in docs/METRICS.md rather than silently left uncited.

## 2026-09-22: Adopt channel-based metric tiers (O/P/A-E/V/X) in docs/METRICS.md

- **Context:** docs/STATE.md's ~59 metric lines were a flat, ungrouped list spanning unrelated tracks (S1 diagnostics, S4 boundary F1, HOE classifier, S6 anchor work, resolution-concordance completeness). User asked whether metrics form a tiered sequence; none existed -- the closest was PLAN.md's acceptance-criteria table, which orders targets, not the other ~46 diagnostics. The project already names anchor-signal channels A-D inline in PLAN.md's S6b session notes (PLAN.md:538-561); user chose to organize the metric tiers by those channels rather than an abstract distance-from-goal scheme.
- **Decision:** Created docs/METRICS.md defining Tier O (outcome/acceptance criteria, sourced from PLAN.md), Tier P (placement/model, cross-channel), Tier A-D (one per anchor channel; D also covers session-boundary/concordance completeness), Tier E (short-resolution anchors via align_short_resolutions.py, currently empty pending integration into scripts/s6b_anchor_harvest.py), Tier V (validation/consistency invariants -- pass/fail, not coverage), and Tier X (exploratory/historical readings that justified a past decision but are not ongoing tracked signals). Mapped every existing docs/STATE.md metric line into exactly one tier. Also specified four not-yet-computed proposals, all deferred to a following step: a Tier-O consecutive-day span table implementing the existing but never-measured 'Global, spans' criterion; a Tier-D per-day K_e drift-comparison diagnostic; a Tier-V nihil-actum invariant (a nihil_actum day must have zero attributed resolutions); and a Tier-P bottleneck diagnostic that reruns the S6 Step-1 oracle technique to decide whether anchor supply (A-E) or the placement algorithm (P) is the next worthwhile investment -- explicitly scoped as an extension of scripts/svz.py review and docs/ITERATION_POLICY.md, not a new orchestrator or DP solver.
- **Reason:** A flat metric list could not answer whether results are tiered, how they relate, or whether anything is missing. Channel-based grouping keeps each metric next to the thing that would move it, and matches the project's own aDNA framing (PLAN.md:11-15): Tiers A-D as anchor/seed classes, Tier P as the aligner whose own resolving-power limit is distinct from input quality, Tier O's spans as a coverage-breadth statistic, Tier V as a negative-control check. The bottleneck diagnostic stays a decision aid feeding the existing review mechanism rather than a parallel system, consistent with docs/ITERATION_POLICY.md's 'advisors and editors, not operators' convention. Only documentation changed this session; the four proposals are next-step work.

## 2026-09-22: Tier V day series parents Tier D K_e drift

- **Context:** docs/METRICS.md specified Tier V (nihil-actum invariant) and Tier D (K_e drift) as separate deferred proposals; state.json had V->O->D. User asked to fill V then D and noted D benefits from V.
- **Decision:** V emits the shared per-day K_e series (k_e_signal=0 on nihil days, invariant_ok gate). D consumes that series only — never rebuilds K_e from concordance. Nihil days are never drift candidates; V violations block those dates. Tier O spans table (still open) should reuse the same V parent.
- **Reason:** Keeps the negative-control (V) upstream of any drift/span reading so a failed invariant cannot silently inflate Tier D/O numbers; k_e_signal avoids treating the 1-row Nihil Actum formula as ordinary low-K_e work.

## 2026-09-22: Short-resolution Step 4 ranking gate fails — baseline saturates

- **Context:** Held-out test ranking (n=20) after live LLM run: baseline top-1 20/20, LLM top-1 8/20, abstention 7/20. Side-plan gate requires LLM to beat strongest deterministic baseline.
- **Decision:** Record GATE_FAIL. Do not promote ranking into production concordance or advance Steps 5-6 on ranking grounds. Deterministic baselines remain the comparison ceiling for this sample; next side-plan move is Step 7 revise/close unless a separately scoped opening-signal experiment is justified.
- **Reason:** Perfect baseline top-1 leaves no measurable LLM gain; abstention and underperformance vs that ceiling cannot clear the documented gate.

## 2026-09-22: Close short-resolution LLM side track (Step 7)

- **Context:** plans/SHORT_RESOLUTION_SIDE_PLAN.md Steps 1-4 done; held-out ranking GATE_FAIL (baseline top-1 20/20, LLM 8/20, abstention 7/20). Promote criteria require beating the strongest deterministic baseline and a gain in low-anchor/merged strata; none met. Steps 5-6 not entered.
- **Decision:** CLOSE the LLM side track. Do not promote summaries, ranking, or opening signals into concordance, S6c, or any production consumer. Keep short_resolution_* experiment datasets registered for audit only. Leave short-resolution-alignment / Tier E (align_short_resolutions.py formula coverage → wire-in vs retire) open as a separate deterministic decision. Reopen LLM work only with a sample where deterministic baselines fail on held-out ranking.
- **Reason:** No measurable LLM gain over a saturated deterministic baseline; further prompt tuning cannot clear the documented gate. Closing avoids sinking Steps 5-6 into an already-failed ranking premise while preserving the distinct Tier E orphan question.

## 2026-09-22: Retire Tier E / short-resolution-alignment with the LLM side-track close

- **Context:** Step 7 closed the short-resolution LLM side plan (ranking GATE_FAIL). Tier E (align_short_resolutions.py formula anchors) was still listed as an open orphan: built once, no metric, not in s6b_anchor_harvest. User judged it off the drawing board with the side track.
- **Decision:** Retire Tier E and mark short-resolution-alignment done/retired. Do not wire formula short-resolution anchors into concordance or S6b harvest without a new measured case that clears a gate against current deterministic channels. Keep the script as historical reference only.
- **Reason:** The side track was the live evaluation vehicle for short-resolution signal; with it closed and Tier E never measured or consumed, keeping the orphan open invites undirected work. Retirement matches the Step 7 rule against promoting unvalidated short-resolution consumers.

## 2026-09-22: Tier O spans measured; Tier P bottleneck automated

- **Context:** metrics-separation-span-table and metrics-bottleneck-diagnostic were the remaining open items from the 2026-09-22 metrics-tier overhaul.
- **Decision:** Built scripts/metrics_separation_span_table.py (parent Tier V k_e_signal) and scripts/metrics_bottleneck_diagnostic.py (oracle vs NW real under unmodified interpolate_positions). Global, spans is unmet at gap in {0,1,2}: 0 qualifying >=30d spans (longest 6/9/9). Separated baseline 1961/11644=16.8% reproduced. Bottleneck reading: oracle_coverage=0.263, real_coverage=0.368, recommended_focus=algorithm_redesign (oracle itself below redesign threshold; real>oracle is the known thin-anchor monotone dodge, same pattern as endpoints density).
- **Reason:** Makes PLAN.md Global, spans and the evidence-vs-algorithm QC move repeatable; confirms placement-model redesign remains the binding constraint under the unmodified interpolator, consistent with the 2026-09-20 S6 Step-1 decision.

## 2026-09-22: Defer severe-collapse autopsy

- **Context:** Tier O unmet (0 spans@30d; longest 6/9/9) with 221 solid days already; 79 severe-collapse days are a disclosed S6c caveat (7.5%), not the binding span-formation problem. session_date_verified now at 71.9% day density.
- **Decision:** Defer the 79-day severe-collapse autopsy. Next: map why solid days fail to form contiguous >=30d spans; consider S6e weight fitting with session_date_verified. Revisit the 79 only if the span map implicates them.
- **Reason:** Solid-day geography and dense Group-D anchors are cheaper levers on Global spans than an autopsy of a disclosed low-localization tail; Group-C phrase evidence already failed as a collapse fix.

## 2026-09-22: Span-gap map: reopen severe-collapse autopsy

- **Context:** metrics_span_gap_map on 221 solid days: 171 breaking gaps; dominant interrupter weak_separation (125/171); 43/171=25.15% of breaking gaps contain a severe-collapse day (0 solid days are themselves severe-collapse; recomputed corpus severe-collapse count=92). Bridge counterfactuals: longest after bridging weak_separation=12d, missing_htr=6d, nihil=9d — all still 0 spans@30.
- **Decision:** Reopen the deferred 79-day severe-collapse autopsy (flag severe_collapse_implicated=True). Do not expect autopsy or single-class bridging alone to meet Global spans: even making all weak_separation transparent caps longest at 12 calendar days. Autopsy is a local quality lever on the dominant interrupter; densifying solids (raising separated_share on weak_separation days) remains necessary for >=30d runs.
- **Reason:** The 2026-09-22 deferral said revisit the 79 only if the span map implicates them; 25.15% of breaking gaps contain collapse days (threshold 25%) and those gaps are almost all weak_separation-dominated (40/43). Recording the bridge ceiling prevents mistaking the autopsy for a Global-spans unlock.

## 2026-09-22: S6c severe-collapse autopsy

- **Context:** Span-gap map reopened the deferred autopsy (severe_collapse_implicated=True, 43/171 breaking gaps). Ran scripts/s6c_severe_collapse_autopsy.py on s4_corpus_paragraph_predictions + metrics_span_gap_map: 92/1140 predicted days (8.1%); cause_class pigeonhole_forced=32, axis_requires_repeats=50, room_on_axis=10; all 92 are weak_separation nonsolids; mean excess over pigeonhole floor=4.3; max_on_paragraph=25.
- **Decision:** Close the autopsy as a diagnostic deliverable (dataset s6c_severe_collapse_autopsy). Do not retune S6c weights or re-wire Group-C for collapse: only 10/92 days have room_on_axis, and 32/92 are pigeonhole-forced (need finer axis, still blocked). Prefer densifying solids on weak_separation days (raise separated_share) as the Global-spans lever; S6e weight fitting with session_date_verified is the next candidate if a placement change is attempted.
- **Reason:** Autopsy confirms collapse is mostly axis-pressure (82/92 pigeonhole or axis_requires_repeats), entirely inside weak_separation interrupters, and Global spans still need converting weak_separation to solid (bridge ceiling 12d). Matching the 2026-09-22 reopen decision's explicit non-goal.

## 2026-09-22: Severe-collapse spot-check: missing text vs axis selection

- **Context:** User hypothesized that max>=7 pile-ups may reflect missing HTR rather than DP failure. Spot-checked P=1 pigeonhole days against concordance + paragraph_axis; quantified all 92.
- **Decision:** Three strata: (1) richer_session_ignored=9 — axis_for_date returns thin same-calendar stubs when concordance already maps to a richer session on a neighboring date (e.g. 1627-07-10: calendar P=1 vs session-3186-num-117 P=31 on 1627-07-12); fix is prefer concordance session when sess_P > same_P. (2) genuinely_thin_session=15 — both calendar and resolved session are P<=1, often inventory 4562 single blobs (hundreds–3k chars for K_e 10–15); real truncated/unsplittable HTR, not a selector bug. (3) thin_axis_no_better_session=56 — short axis with no richer session available. Do not treat all severe-collapse as missing text; next cheap code fix is (1).
- **Reason:** Direct evidence from axis + concordance for the worst P=1 days; 8/32 pigeonhole_forced are the selector bug, 15/32 are genuinely thin.

## 2026-09-22: axis_for_date must not treat non-empty same_day as a day cutoff

- **Context:** Current axis_for_date returns same-calendar-day paragraphs whenever the list is non-empty, falling back to concordance only if empty. Spot-check showed 9 collapse days where a thin calendar stub beat a richer neighbor-dated resolved session. User correction: that gate is wrong by any standard unless we have a proven day cutoff — calendar presence is not proof the sitting belongs on that date.
- **Decision:** Do not keep the 'if same_day: return same_day' rule. Prefer the concordance-resolved session whenever day_status is resolved_auto (or another proven cutoff such as session_date_verified / ledger T), and only use same-calendar-day axis when that cutoff says the calendar date is trusted OR when no resolved session exists. The weaker 'prefer whichever has more paragraphs' heuristic is a stopgap at best, not the acceptance criterion — richness is a symptom of the bug, not the authority relation.
- **Reason:** Concordance is the accepted day-mapping track; non-empty same-day HTR is not a verified date assignment under known session/date drift.

## 2026-09-23: Register S4f session-date decisions + approved merge

- **Context:** s4_session_date_mapping_decisions and predictions_approved were registered in the manifest but MISSING on disk; the 2026-09-21 UI export lived only at data/import/s4_session_date_mapping_decisions.json (9 decisions / 301 queue).
- **Decision:** Copied the import export to the registered output path and ran merge_session_date_mapping_decisions.py --reviewer rikhoekstra. Approved artifact: 4916 ledger rows (4 approved, 4 abstained, 1 deferred, 4907 unreviewed). Relaxed merge validation so defer/no_match may carry a UI-highlighted selected_session_id (still nulled in output).
- **Reason:** Closes the data_io.check gap so concordance/day-status consumers can read human-approved mappings when present; does not re-open the unfinished 301-row review queue.

## 2026-09-23: axis_for_date fixed and remeasured

- **Context:** Implemented the 2026-09-22 `axis_for_date` decision: `scripts/s4_corpus_paragraph_predictions.py` now prefers the concordance-resolved session (`day_status == "resolved_auto"`) over a non-empty same-calendar-day stub, falling back to the same-day axis only when no resolved session is recorded or its axis is empty. 6 tests updated/added (`tests/test_s4_corpus_paragraph_predictions.py`, 8/8 passing); full suite 449 passed.
- **Decision:** Re-ran the full pipeline in sequence — `s4_corpus_paragraph_predictions` → `metrics_span_gap_map` → `s6c_severe_collapse_autopsy` — and accept the new numbers as current. Coverage unchanged (1,140 predicted / 454 `missing_htr` abstained — this fix changes *which* axis a day uses, not *whether* one is found). Of 1,140 predicted days, 1,057 now route through the resolved-session path instead of the same-day one, but only **77** actually change axis content (the other 980 resolve to the same underlying session either way — reported separately so the headline number isn't inflated by cosmetic routing changes). Severe-collapse days **92 → 83** (max pile-up 25 → 16); breaking gaps containing a collapse day **43 → 40** (25.15% → 23.39%, dropping `severe_collapse_implicated` from **True to False** against the 0.25 threshold — reversing the 2026-09-22 "reopen" call from the pre-fix numbers).
- **Reason:** Directly confirms the 2026-09-22 spot-check's prediction (9/92 collapse days were `richer_session_ignored`) — 9 fewer severe-collapse days after the fix, matching exactly. `severe_collapse_implicated` flipping to False means the autopsy's own reopening rationale no longer holds on refreshed data; further severe-collapse work should re-justify itself against the new 83-day/40-gap figures, not the stale 92/43 ones. Global-spans bridge-ceiling reasoning (longest 12 calendar days after bridging `weak_separation`) is unaffected — re-ran `metrics_span_gap_map` and it still reports 0 spans@30 under all interrupter classes.

## 2026-09-23: resolution_concordance_1626_1630 was stale, gating the entire Tier O separation baseline

- **Context:** Ran a cheap weak_separation-headroom diagnostic (min(1, paragraph_count/k_e_signal) structural ceiling per day) to decide whether densifying weak_separation needs a placement-quality fix (S6e) or a finer axis. Result: 91.4% of weak_separation days were headroom_available but median separated_share on those days was exactly 0.0 even where paragraph_count==k_e_signal (ceiling=1.0) -- implausible for a placement-quality gap alone. Traced to resolution_concordance_1626_1630's own provenance: created 2026-09-17T15:10, three predictor rebuilds behind s4_corpus_paragraph_predictions (S6c DP eliminating 777 insufficient_entity_anchors abstentions on 2026-09-21, the concordance-fallback wiring same day, and the axis_for_date fix on 2026-09-23). Only 113/11513 resolutions on sampled headroom dates had paragraph attribution; the rest showed paragraph_prediction_status=abstained even though the live predictions.jsonl record for that date was status=predicted.
- **Decision:** Re-ran scripts/s4_resolution_concordance.py (its own docstring: pure assembly, no new matching/alignment logic; 26s) then the full Tier O chain (metrics_nihil_actum_invariant -> metrics_separation_span_table -> metrics_span_gap_map -> s6c_severe_collapse_autopsy -> metrics_weak_separation_headroom_diagnostic). Rows with paragraph attribution jumped from a small fraction to all 13354 resolved_auto rows. PLAN.md's Global-primary headline moved 1961/11644=16.8% -> 8015/11644=68.8%, clearing the >=50% primary criterion (stretch >=75% not yet, gap ~718). Solid days 221/1594 -> 693/1594. Global spans@30 gap=2: 0 spans -> 4 spans/177 days (criterion needs >=6 spans/>=180 days, so close but not met). severe_collapse_implicated stays False. No new algorithm/modeling work was involved -- this closes an orphaned-track staleness gap of the same shape flagged repeatedly this project (STEP_S6 section 1(b)).
- **Reason:** The headroom diagnostic's own near-zero separated_share on ceiling=1.0 days was too implausible to be a genuine placement-quality signal, so it was checked against its data source before being trusted or used to scope S6e. docs/CANDIDATE_SCORING_AND_CONCORDANCE.md's own design intends paragraph attribution to track s4_corpus_paragraph_predictions live; nothing in that doc says concordance should be a frozen snapshot, so refreshing it is a data-hygiene fix, not a policy change.

## 2026-09-23: Group-B position_scores wiring measured negative, not wired into live predictor

- **Context:** PLAN.md 2026-09-23 scoped wiring entity_surface_matches_1626_1630 into segment_day's position_scores (same mechanism as the already-negative Group-C phrase hits), tested cheaply on the 19/21 scoreable gold days through s4_corpus_paragraph_predictions.predict()'s own codepath before any corpus-wide rerun. Metric corrected per user instruction this session: scored at PARAGRAPH tol0/1/2 (docs/DECISIONS.md 2026-09-21 'S6c target metric rescoped'), not character-offset tolerance.
- **Decision:** Do not wire Group B into predict()'s live position_scores. Baseline (no evidence) tol0=0.644; with Group B tol0=0.555 (worse), tol1 flat at 0.790, tol2 improves to 0.840. Net negative at the primary paragraph-exact granularity. predict()'s new position_scores parameter (added this session) stays unused by default -- kept only so scripts/s6_group_b_position_scores_eval.py can test through the real codepath without duplicating it.
- **Reason:** Same root cause as the Group-C finding: entity_surface_matches records what entities are PRESENT in a resolution's text, not where a resolution OPENS -- feeding it through the same phrase_weight/dispersion bonus-vs-even-split tradeoff segment_gap uses for opening-phrase hits pulls cuts toward entity-dense paragraphs irrespective of whether they start a resolution. Two independent evidence sources (Group B and Group C) have now failed the same way through the same scoring mechanism, which is stronger evidence the mechanism itself -- not the evidence choice -- is the limiting factor. Densifying weak_separation via position_scores tuning is likely exhausted without a smarter in-gap signal (e.g. phrase-hit-style start-specific evidence only, or per-entity role weighting, both already named as untried in the 2026-09-18 split-POC session notes).

## 2026-09-23: Correction: Group-B and Group-C position_scores regressions are not the same magnitude or mechanism

- **Context:** Follow-up to the 2026-09-23 'Group-B position_scores wiring measured negative' decision, which claimed Group B and Group C 'fail the same way through the same scoring mechanism' -- the user pushed back on that as implausible and asked for concrete examples. Re-ran the same day-by-day predicted-position comparison (baseline vs. with-evidence) for BOTH groups, restricted to the 19-21 scoreable gold days, through predict()'s real codepath.
- **Decision:** The claim was an overreach; retracted. Group B: 11/19 scoreable days changed prediction, ALL 11 strictly worse (net -21 true-positive cuts across those days, e.g. 16->12 on 1630-03-20). Group C: 8/21 days changed, 6 unchanged-TP / 2 worse / 0 better (net -2 true-positive cuts total) -- an order of magnitude milder. Traced Group B's larger damage to a concrete, identified cause: entity_surface_matches_1626_1630 is keyed by flat RESOLUTION id, not paragraph id, so every axis paragraph belonging to a multi-paragraph flat resolution gets an IDENTICAL bonus (verified directly on 1626-01-08: resolution-12 spans 4 axis paragraphs, resolution-2 spans 3, all sub-paragraphs share one score). Combined with near-saturated match rates (12/13 resolutions matched at 85-100 confidence on that day), the bonus is a near-flat high plateau across almost the whole axis -- it nudges an already-good even-split default toward arbitrary high-score paragraphs uncorrelated with true boundaries. Group C's phrase-hit evidence, despite similarly high paragraph coverage (6-17 of 8-20 paragraphs per day), is computed AT paragraph granularity to begin with, which is the more likely reason its damage stays small.
- **Reason:** Correct diagnosis matters for what to try next: this is evidence that position_scores evidence CAN be near-neutral (Group C) or even beneficial if it is genuinely paragraph-localized, not that in-gap content evidence is a dead end. The dominant fixable variable is Group B's flat_id-level granularity mismatch, not the position_scores mechanism itself. Do not cite the retracted 'mechanism exhausted' framing going forward; see this entry instead.

## 2026-09-23: entity_surface_matches' one-sided 'new' recoveries are almost entirely ungrounded (1.6%) -- confirms two-sided design is required, not optional

- **Context:** Follow-up to the 45.2% one-sided headroom finding (2026-09-23) and the user's request to run the decisive two-sided check before deciding whether to extend align_session's lookup. Sampled 40 random dates (seed=42) across the full 1626-1630 corpus, resolved each date's enriched resolutions' own known entities via build_alignment_new.py's resolve_enriched_entities (already-proven code, unmodified), and checked how many of the 243 sampled 'new' entity_surface_matches names match ANY specific enriched resolution that day (not just float unattached).
- **Decision:** Only 4/243 (1.6%) are grounded to a specific enriched resolution's own entity list; 98.4% are floating. Sanity-checked directly (1626-01-19): genuinely-grounded enriched entities are small, specific place names (Oudenbosch, Kempenland, Zevenbergen, Hooge Zwaluwe); the floating flagged names are large, common geographic/political terms (Holland, Zeeland, Engeland, Amsterdam, Groningen, Algiers) that plausibly appear as incidental context in most any resolution's narration, not as that resolution's own subject. This is not a bug in the check (resolve_enriched_entities verified working correctly on real data) -- it is a real property of entity_surface_matches' recoveries.
- **Reason:** Retroactively explains why feeding Group B's one-sided presence signal into segment_day's position_scores regressed so badly earlier this session: a signal that is 98.4% ungrounded fires as near-uniform noise, exactly the near-saturated-high-plateau pattern already diagnosed on 1626-01-08. It also decisively validates the 'one unequivocal entity-overlap layer' design discussed with the user: any consolidated table MUST require genuine two-sided grounding (a specific candidate enriched resolution's own resolved entities present in the flat text), not one-sided presence-in-text, or 98%+ of a naive fuzzy-recovery pass would need to be treated as anchor-quality evidence when it is not. Do not treat entity_surface_matches (or s4_fuzzy_surface_form_scan) as anchor-ready without this two-sided filter applied first.

## 2026-09-23: Day-level grounding of Group-B partially recovers but does not close the regression

- **Context:** docs/DECISIONS.md 2026-09-23 found entity_surface_matches' one-sided 'new' recoveries are 98.4% ungrounded (1.6%/243 match a specific enriched resolution's own known entities), explaining Group-B position_scores' measured regression (tol0 0.644->0.555). Built scripts/s6_group_b_grounded_position_scores_eval.py to test the implied fix: restrict Group-B's bonus to matches whose canonical_name is grounded (present in the UNION of that calendar date's own enriched resolutions' resolved entity sets via resolve_enriched_entities), same 19/21 gold-day harness as the ungrounded eval.
- **Decision:** Day-level grounding is a real but partial fix, not sufficient to wire in: tol0 F1 0.555 (ungrounded) -> 0.588 (grounded), still below the 0.644 no-evidence baseline; tol1 0.790->0.784 (slightly worse); tol2 0.840->0.824 (still better than baseline's 0.807 but worse than ungrounded). Do not wire grounded Group-B into the live predictor on this result. 101/181 (56%) of flagged paragraphs survive day-level grounding, far above the 1.6% grounded share the corpus-wide 40-day two-sided sample found -- day-level grounding is coarser than per-resolution grounding (it only checks whether ANY enriched resolution that date claims the name, not the specific paragraph's true owner), so it still lets through same-day cross-resolution false positives.
- **Reason:** Confirms groundedness was a real contributing cause (partial recovery in the predicted direction) but not the whole story -- the day-level check is too permissive on multi-resolution days where several enriched resolutions share common entity names (e.g. 'Holland', 'Staten Generaal'-adjacent terms). A tighter per-paragraph/per-candidate-assignment grounding check would need to know the placement the predictor is trying to produce, which is circular for a pre-computed position_scores bonus; the next viable direction (if this lever is pursued further) is per-entity specificity weighting (rare/short place names like the 1626-01-19 spot-check's Oudenbosch/Kempenland vs common ones like Holland/Amsterdam), not day-level presence.

## 2026-09-23: Span-gap pairwise bridging: even the most generous defensible relaxation falls short of the Global-spans stretch criterion

- **Context:** svz.py review's own pick for this session: map why solid days (693/1594) fail to form >=30-day spans, since severe_collapse_implicated flipped False and its reopening rationale no longer held. Generalized metrics_span_gap_map.py's single-class bridge counterfactual to pairwise combinations of the three populated nonsolid_class values (missing_htr 329, nihil_actum 127, weak_separation 445).
- **Decision:** Single-class bridges: nihil_actum alone -> longest 12->31d (1 span/31d); missing_htr alone -> no change (12d); weak_separation alone -> 20d. Pairwise: missing_htr+nihil_actum (the two classes with no genuine algorithmic-failure evidence -- no business done, or no HTR text to judge) -> still only 31d/1 span, short of the >=6 spans/>=180 days stretch criterion. Only missing_htr+weak_separation jointly clears it (51d longest, 9 spans, 366 days, criterion_met=True) -- but that requires bridging weak_separation, a REAL measured placement-quality gap, not an evidence gap. Conclusion: even treating every day with no basis to judge failure as non-breaking isn't enough; weak_separation (placement quality) must also improve for the stretch criterion to be reachable. Do not pursue cheap bridging as a substitute for placement-quality work.
- **Reason:** Confirms, from an independent angle (span geography, not the position_scores gold-day harness), the same conclusion the exhausted position_scores experiments (Group B/C, 3 variants) already pointed to: placement quality on weak_separation days is the binding constraint on Global spans, not data/axis coverage. The two remaining candidate levers (per-paragraph .find()-relocated Group-B evidence; the full two-sided entity-overlap consolidation table) are both already flagged as multi-session investments needing an explicit go/no-go, not a default next action.

## 2026-09-23: resolved_session_id has no cross-date uniqueness constraint -- likely driver of weak_separation clustering, found via user-proposed windowed K_e-vs-K_f drift check

- **Context:** User asked (1) where's the biggest expected improvement, and (2) whether a windowed K_e-vs-HTR-count comparison (to detect session/date shift) is already investigated, proposing it be scoped to specific windows rather than run corpus-wide. Built scripts/metrics_ke_kf_window_drift_diagnostic.py (10/10 tests), scoped to metrics_span_gap_map's 13 longest weak_separation-dominated breaking gaps: per-day residual (k_e - paragraph_count), lag-1 autocorrelation, and sign-flipping adjacent-pair share.
- **Decision:** 12/13 flagged windows show a shift_candidate signature (alternating over-/under-attribution between adjacent days), only 1/13 a uniform capacity shortfall. Spot-checked the #1-ranked window (1627-03-25..1627-04-10, 15 nonsolid days) directly against resolution_concordance_1626_1630: 1627-03-27 and 1627-03-29 both carry resolved_session_id=session-3186-num-55 (identical paragraph_count=53); 1627-04-01 and 1627-04-03 both carry session-3186-num-57 (identical paragraph_count=77) -- two different enriched calendar dates independently resolved to the SAME underlying HTR session. Traced to source: scripts/s4_day_status_resolution.py's resolve_row() picks each ledger row's (i.e. each enriched date's) top_candidate_session_id from s4_candidate_scoring_predictions independently, per-date, with no constraint that a session can be claimed by only one date. Quantified corpus-wide: 68/1053 resolved_auto sessions (6.5%) are claimed by 2+ enriched dates, 153 dates (~10% of resolved_auto dates) involved. Recommend this as the next concrete unit of work over further position_scores tuning: decide a uniqueness policy (best-score-wins per session, with the loser falling back to its next candidate or to uncertain) and re-run the day-status/concordance chain to measure the effect on weak_separation and Global spans.
- **Reason:** This is a structural mapping gap with concrete before/after evidence (not a data-quality ceiling like missing_htr, and not another position_scores evidence variant -- three of those already failed through the same in-day-placement mechanism). It directly explains why the worst weak_separation stretches cluster rather than scattering randomly, and is independently corroborated by three prior signals this project already had: the axis_for_date richer_session_ignored bug (9 cases, fixed 2026-09-23), s6b_session_fingerprint_match's 62% drifted-offset rate, and the unreviewed 301-row session-date mapping queue. Answers the user's 'where's the biggest expected improvement' question with a specific, sized, traced mechanism rather than a general prioritization argument.

## 2026-09-23: Corpus-wide collision-vs-weak_separation crosstab confirms and sizes the mechanism: 27.6% of weak_separation days are collision-involved

- **Context:** Follow-up to the 2026-09-23 'resolved_session_id has no cross-date uniqueness constraint' decision, which spot-checked one window and sampled 13 windows via a heuristic (autocorrelation/sign-flip) shift-signature score. User asked to check specifically against the weak_separation days blocking spans, not just a window sample -- extended scripts/metrics_ke_kf_window_drift_diagnostic.py with session_collision_dates() and collision_weak_separation_crosstab() (2 new tests, 12/12 passing) to join ALL resolved_session_id collisions against the full Tier O day series (1,138 resolved_auto days), not just the 13 flagged windows.
- **Decision:** Collision dates (n=153) are weak_separation 80.4% of the time vs 32.7% for non-collision dates -- a 2.5x effect size, not a coincidence of the window sample. Population-level: 123/445 (27.6%) of ALL weak_separation days corpus-wide carry a session-collision. Registered output now includes a corpus_collision_crosstab record alongside the per-window signatures.
- **Reason:** Answers the user's 'check specifically against the weak_separation days blocking spans' request rigorously rather than by window-sample extrapolation. Over a quarter of the exact population blocking Global spans is explained by one traced, fixable mapping bug in scripts/s4_day_status_resolution.py's resolve_row() (no cross-date uniqueness constraint on top_candidate_session_id) -- strengthens the case for prioritizing a uniqueness-policy fix there over further position_scores tuning, which only ever addressed in-day placement and could never reach this 27.6%.

## 2026-09-23: Uniqueness policy wired into resolve_row(); re-measured -- real but partial effect, paragraph-level separation not yet propagated

- **Context:** Follow-up to the two decisions above. Before this fix, s4_day_status_resolution.py's resolve_row() read each candidate-scoring row's top_candidate_session_id independently with no cross-date constraint. Checked first whether the collision was specific to candidate_scoring or also present in ledger_direct (T/E) rows: querying s4_day_status_resolution directly found BOTH sources collide (candidate_scoring 123/129 resolved_auto rows collision-involved; ledger_direct 87/1,109) -- but the traced mechanism and both prior decisions scoped the fix to candidate_scoring's top_candidate_session_id specifically, so ledger_direct collisions are left untouched and out of scope for this pass (T/E rows aren't ranked picks -- there's no "next candidate" to fall back to, and a session legitimately spanning ledger rows for adjacent dates via overlapping inventory metadata can't be ruled out without separate investigation).
- **Decision:** Added `deduplicate_session_claims()` (scripts/s4_day_status_resolution.py, 6 new tests, 16/16 passing) as a preprocessing pass over `s4_candidate_scoring_predictions` before `resolve_row()` runs: global greedy assignment over every (row, ranked_candidate) pair sorted by descending combined_score -- highest score claims its row and session first; any later pair naming an already-claimed row or an already-claimed session is skipped, which is exactly "loser falls back to its own next-ranked confident candidate" since that pair appears later in the same sorted list; a row that exhausts every confident candidate becomes `low_confidence=True` (-> `uncertain` in `resolve_row`, unchanged). No rescoring needed -- `ranked_candidates` was already persisted per row by `s4_candidate_scoring_batch.py`. `resolve_row()` itself is unchanged.

  Re-ran the chain (`s4_day_status_resolution` -> `s4_resolution_concordance` -> `metrics_nihil_actum_invariant` -> `metrics_separation_span_table` -> `metrics_span_gap_map` -> `metrics_ke_kf_window_drift_diagnostic`), then, since `axis_for_date` (`s4_corpus_paragraph_predictions.py`) itself falls back to `resolution_concordance_1626_1630`'s `resolved_session_id`, re-ran `s4_corpus_paragraph_predictions` too (cheap, ~2.5s, per its 2026-09-21 precedent) and repeated `s4_resolution_concordance` + the Tier O chain a second time so paragraph attribution reflects the corrected session mapping, not the stale one. All told <15s of compute.

  `resolved_auto` day-status rows 1,238 -> 1,202 (36 rows, mostly collision losers with no confident fallback, moved to `uncertain`). Collision dates corpus-wide 153 -> 132 (-13.7%); `weak_separation` days 445 -> 430 (-15); `weak_separation` collision share 27.6% -> 23.9% (123/445 -> 102/426) -- the collision mechanism itself is fixed as designed, and the remaining 132 collision dates are consistent with the untouched 87 ledger_direct collisions (out of scope, see above) plus residual candidate_scoring ties on rows that had zero fallback candidates. 75 days' resolved axis changed content after the predictor refresh.

  **But the headline metrics did not move**: after the full two-pass propagation, Global-primary separation is 8,015/11,644 = 68.8% of ceiling -- identical to the already-recorded 2026-09-23 baseline, not the 8,020 seen after only the first (predictor-stale) pass. Spans@30 unchanged (4 spans/177 days at gap=2, longest 60d). `weak_separation` day count in `metrics_span_gap_map` also held at 430 across both passes, i.e. the predictor refresh changed *which* axis 75 days use but not enough of them flipped from weak to solid (or vice versa) to move the count.
- **Reason:** The collision-count and weak_separation-collision-share drops are genuine, measured, and mechanism-consistent -- the fix does what it was designed to do (68/1,053 collided sessions is no longer possible for the candidate_scoring path). But collision-freeness at the day-status level is necessary, not sufficient, for a date's paragraph-level placement to actually improve: a date that stops colliding still inherits whatever `segment_gap`'s in-gap DP produces on its now-uncontested session, and that placement-quality layer is exactly what three prior `position_scores` experiments (Group B/C, this session's cycle) already found near-exhausted. This is a second, independent confirmation of the same standing conclusion (docs/DECISIONS.md 2026-09-23 "Span-gap pairwise bridging"): **placement quality on `weak_separation` days, not which session a date maps to, is now the binding constraint** -- fixing the mapping bug was necessary to rule it out as a confound, but it was not itself the lever that moves Global spans. Do not expect further mapping-uniqueness work (e.g. extending this policy to ledger_direct T/E collisions) to move the headline numbers either, absent new evidence it targets placement quality specifically.

## 2026-09-23: Global-spans downgraded from readiness gate; S6 segmentation DP greenlit for the stretch gap

- **Context:** Follow-up to the uniqueness-policy fix (docs/DECISIONS.md 2026-09-23), which confirmed the collision mapping bug was real but did not move Global-primary (68.8% of ceiling, unchanged) or spans (4 spans/177 days, unchanged). Asked the user whether the >=30-day contiguous-span stretch criterion actually matters for the downstream use case before deciding whether to invest in the multi-session S6 segmentation DP. User confirmed: the downstream consumer only needs per-resolution placement, not 30-day contiguous coverage.
- **Decision:** (1) Global-primary/stretch (per-resolution placement, 68.8% of ceiling, MET at primary/not at 75% stretch) is the metric that matters for readiness; Global, spans is downgraded from a readiness gate to a tracked-but-non-blocking metric -- do not scope future work specifically to move it. (2) User greenlit the S6 segmentation DP (scripts/s6c_gap_segmentation.py's segment_gap/segment_day, s6-anchor-chain-alignment track) as the one credible untried lever to close the 718-resolution stretch gap, since position_scores evidence tuning (Group B/C, 3 variants) and the session-uniqueness fix are both now measured exhausted/flat on this metric.
- **Reason:** Keeps future sessions from re-litigating or reopening span-bridging work (already shown to plateau at 12-60 calendar days regardless of interrupter class bridged) now that it's confirmed non-binding for the real use case. Records the go decision on the S6 DP explicitly, per docs/ITERATION_POLICY.md's rule that a multi-session investment needs an explicit go/no-go from whoever reads the report, not a default pickup -- that call is now made and traceable.

## 2026-09-23: Per-paragraph .find()-relocated Group-B evidence measured negative -- granularity-mismatch hypothesis does not recover the regression

- **Context:** User picked lever (a) of the three untried S6 placement-quality candidates (docs/DECISIONS.md 2026-09-23 greenlight): per-paragraph .find()-relocated Group-B evidence, which docs/DECISIONS.md's 'Correction' entry diagnosed as the dominant fixable variable behind Group-B's -21 TP regression (entity_surface_matches_1626_1630 is keyed by flat resolution id, so every sub-paragraph of a multi-paragraph resolution shares one identical bonus).
- **Decision:** Built scripts/s6_group_b_relocated_position_scores_eval.py (group_b_relocated_position_scores: exact substring, fuzz.partial_ratio fallback at build_entity_surface_matches.py's own validated threshold 85, applied per axis paragraph's own text instead of per whole flat-resolution text), 4 new tests (4/4 passing), registered s6_group_b_relocated_position_scores_eval in the manifest. Same 19/21 gold-day harness through predict()'s real codepath. Result: tol0 F1 0.543 (worse than blanket Group-B's 0.555, further from baseline 0.644); tol1 0.773 (worse than blanket 0.790); tol2 0.840 (tied with blanket). A follow-up ad hoc exact-only variant (fuzzy fallback removed entirely) scored 0.560 -- still net negative and barely above the fuzzy-relocated result. Do not wire either variant into the live predictor.
- **Reason:** Spot-checked 1626-01-08 (the same day docs/DECISIONS.md's Correction entry used): relocation correctly dropped the bonus on 2/18 paragraphs where the name was genuinely paragraph-level absent, but left it on almost all others, because most flagged names recur (genuinely or via permissive short-string fuzz.partial_ratio) across nearly every paragraph of their source resolution, not just the one that originally justified the flat-level match. The exact-only control isolates fuzzy-fallback noise as the cause and rules it out: even zero-false-positive-risk exact relocation stays net negative. This closes lever (a) as tested -- the granularity-mismatch diagnosis was correct as far as it went (Group B's damage genuinely traces to flat-id-level bonus sharing) but fixing it is not sufficient to recover the baseline, meaning the position_scores bonus-vs-even-split mechanism itself remains the dominant limiting factor, consistent with the standing 2026-09-23 conclusion. Remaining untried levers from the same greenlit list: (b) per-entity role/type weighting in segment_gap's cost function, (c) the full two-sided entity-overlap consolidation table -- next session should pick one via explicit go/no-go, per docs/ITERATION_POLICY.md.

## 2026-09-23: Gap-candidate-grounded Group-B tested (lever c, cheap form) -- ties day-level grounding exactly, closing position_scores tuning

- **Context:** User go/no-go (2026-09-23) picked lever (c), the full two-sided entity-overlap consolidation table, as the next untried S6c placement-quality lever after (a) per-paragraph .find()-relocation was closed the same day (docs/DECISIONS.md). Building the full 4-phase consolidation table (unify build_windowed_overlap.py/build_per_overlap.py/entity_surface_matches/s4_fuzzy_surface_form_scan into one table read by both align_session/lookup and build_alignment_new.py::resolve_shared_entities) is a genuine multi-session migration, so ran its cheap gold-day-harness precursor first per this cycle's own methodology: does grounding Group-B's bonus to a GAP's specific candidate resolution(s) -- the tightest two-sided grounding possible without the full table -- beat the already-tested day-level grounding (0.555->0.588)? Non-circular: segment_day's count-constrained DP already knows which enriched-resolution indices occupy a gap before choosing where, from the gap's count alone, so scripts/s6_group_b_candidate_grounded_position_scores_eval.py (5 new tests) recomputes that same anchor backbone read-only and scopes resolve_enriched_entities per gap instead of per day.
- **Decision:** Do not build the full two-sided consolidation table on this evidence. Gap-candidate grounding ties day-level grounding EXACTLY at tol0 (0.588 vs 0.588; candidate_id_match_rate=1.0, so this isn't a data-matching artifact) and is marginally worse at tol1 (0.779 vs 0.784), marginally better at tol2 (0.829 vs 0.824) -- a wash, not an improvement, both still well below the 0.644 no-evidence baseline. Closes lever (c) as tested. Combined with lever (a)'s four exhausted variants (blanket/day-grounded/relocated/exact-relocated, all also negative), position_scores tuning is now exhausted across five tested variants total. Only lever (b) (per-entity role/type weighting in segment_gap's cost function) remains untried from the 2026-09-23 greenlit list; next session should either try (b) or accept the current S6c result (68.8% of ceiling, Global-primary MET) as this track's outcome, per docs/ITERATION_POLICY.md.
- **Reason:** The result is informative precisely because it's a null result at maximum achievable precision: if the tightest possible grounding (per-gap-candidate, matched 87/87=100%) can't beat the coarser day-level version, the full consolidation table -- which would deliver exactly this same per-candidate grounding property via more machinery, at multi-session cost -- has no remaining headroom to find. The binding constraint is confirmed to be the position_scores bonus-vs-even-split mechanism itself (entity presence signals content, not resolution-opening position), not evidence granularity at any level tested so far.

## 2026-09-23: S6c position_scores tuning closed: lever (b) not attempted, user accepted current result

- **Context:** Five independent position_scores variants for Group-B evidence in segment_gap (blanket 0.555, day-grounded 0.588, relocated 0.543, exact-relocated 0.560, gap-candidate-grounded 0.588) all tested below the 0.644 no-evidence baseline on the 19/21 gold-day paragraph-tol0 harness. Group-C phrase-hit evidence failed the same way earlier (0.644 -> 0.622). Only lever (b) -- per-entity role/type weighting in segment_gap's cost function -- remained untried from the 2026-09-23 greenlit list.
- **Decision:** Do not attempt lever (b). Close S6c / s6-anchor-chain-alignment as done, accepting the current S6c result: Global-primary 68.8% of ceiling (8,015/11,644), which already meets the >=50% criterion. position_scores tuning is exhausted.
- **Reason:** User call: five failures through the same bonus-vs-even-split mechanism (two independent evidence sources, Group B and Group C) is strong enough evidence that the mechanism itself is the limiting factor that a sixth variant (role/type weighting) is unlikely to recover the baseline. Global-primary is already MET; further tuning session budget is better spent elsewhere per docs/ITERATION_POLICY.md.

## 2026-09-23: fuzzy-surface-form-scan: close, leverage measured and found insufficient

- **Context:** Task's own recorded goal was 'a newly_recovered entity count, plus an explicit decision on whether any downstream consumer justifies the 7-9h scan.' The scan finished 2026-09-21 (189,764 rows) but sat unjudged in state.json while s6-anchor-chain-alignment independently wired its exact output in as Group B of s6b_anchor_harvest.py and tested it five ways in segment_gap's position_scores (blanket 0.555, day-grounded 0.588, candidate-grounded 0.588, .find()-relocated 0.543, exact-only-relocated 0.560 -- all below the 0.644 no-evidence baseline) and as anchor supply for insufficient_entity_anchors (only 18/777 abstentions, 2.3%, gained a usable anchor from it -- since mooted, S6c eliminated that abstention category entirely by replacing interpolate_positions).
- **Decision:** Close fuzzy-surface-form-scan as done. newly_recovered=167,599/189,764 (88.3%) confirms the scan itself ran correctly, but every downstream use already tested (position_scores evidence, anchor supply) returned negative or negligible results -- the same verdict the task's own notes anticipated for the cheaper scoped entity_surface_matches version. No further work is scoped against this output.
- **Reason:** Avoids re-litigating a lever that two independent measurement paths (S6b anchor-supply diagnostic, S6c position_scores tuning) already closed under a different task id; the task was only left open because nobody wrote the answer back to fuzzy-surface-form-scan's own entry.

## 2026-09-23: S4: close, both open items resolved and remaining work absorbed by s6-anchor-chain-alignment

- **Context:** S4's task entry and docs/STATE.md Next Actions still listed two open items from 2026-09-18, both already answered the same day per PLAN.md's own session narrative but never written back: (1) boundary F1 stagnant at 0.576/0.727 despite the snap-collision fix -- explained same-day as a structural ceiling (gold's own boundary annotations repeat one paragraph_stream_index across multiple distinct cut points on 16/17 eligible days with any boundary; max reachable TP is bounded by distinct ref values, and both tol0/tol2 already sat at that ceiling before and after the fix, docs/DECISIONS.md 2026-09-18). (2) the 2 gold dates with zero boundary_gold_paragraph_axis records -- explained same day as the accepted missing_htr structural ceiling (both k_e=1,k_f=0,flat_ids=[], docs/DECISIONS.md 2026-09-18), not a bug. Separately, all of S4's live development since (paragraph-axis predict_day/predict, the segmentation DP, corpus-wide coverage) was carried out and metric-tracked under s6-anchor-chain-alignment (S6a char axis, S6b anchor harvest, S6c segmentation DP replacing interpolate_positions), which closed 2026-09-23 with Global-primary MET (68.8% of ceiling) and stretch not pursued further.
- **Decision:** Close S4 as done. Its two open items were resolved 2026-09-18 (structural ceilings, not bugs) and its live metrics (predicted-day coverage, boundary F1) are superseded by s6-anchor-chain-alignment's numbers, which already closed. No standalone S4 work remains; docs/STATE.md's 'S4' row should be read as historical -- current segmentation numbers live under s6-anchor-chain-alignment.
- **Reason:** S4 and s6-anchor-chain-alignment had been tracking the same underlying pipeline under two task ids since S6a/S6c started editing s4_paragraph_axis_baseline.py and s4_corpus_paragraph_predictions.py directly; leaving S4 inprogress with stale 2026-09-18 notes made svz.py review flag it as a live decision every session even though nothing about it is still open.

## 2026-09-23: track-b-name-matching: close, user judgment call -- not expected to pay off

- **Context:** track-b-name-matching (soundex/Levenshtein reranker for person-name matching, B1 preprocessing / B2 soundex index / B3 structural reranker on TF-IDF top-k) was design-only in PLAN.md, never started, and had no recorded metric -- svz.py review's own ranking bonus for unmeasurable tracks surfaced it as the top recommendation on 2026-09-23 purely because it couldn't be judged, not because of any measured leverage.
- **Decision:** Close track-b-name-matching without starting it. User call: not expected to be worth the effort. No further design or implementation work scoped against it.
- **Reason:** User's explicit judgment, made when this track was surfaced as the next-step recommendation. Recorded so svz.py review stops resurfacing an unmeasurable track as artificially high-priority and so a future session doesn't re-open it without a new reason to expect it would pay off.

## 2026-09-24: Local criterion MET; corpus-wide 11,644 ceiling reproduced as a script for the first time

- **Context:** PLAN.md's Local acceptance criterion (any inventory-year reaching >=50% of its own ceiling) had never been computed -- flagged this session as the one untried angle on the canonical goal, alongside closing track-b/track-c and retiring ner-finetuning. Separately, docs/METRICS.md's 'Known gaps' section flagged the cited 11,644 corpus-wide pigeonhole ceiling as unreproducible by any script in the repo (docs/DECISIONS.md 2026-09-22). Built scripts/metrics_local_inventory_ceiling.py: per-date ceiling = min(k_e, same-calendar-day raw paragraph_count from paragraph_axis_1626_1630), summed per resolution_concordance_1626_1630's resolved inventory_id (one value per date, verified single-valued); separated counts reuse metrics_separation_span_table.count_separated_per_day unchanged. Computed corpus ceiling exactly matches the cited 11,644, and no-HTR date count exactly matches the cited 535 -- both asserted in the script so future drift is caught, not just a one-time coincidence.
- **Decision:** Local criterion is MET: all 5 main annual inventories (3185-3189) clear 50% of their own ceiling (63.9%-72.3%). The 2 non-annual inventories (4562, 4861 -- multi-year 'secret resolution' series) do not (10.5%, 0.0%) but were never the worked-example candidates PLAN.md's Local row was written for (structurally near-zero same-day HTR under this axis, consistent with 4562's already-documented anchor-starved status). Registered metrics-local-inventory-ceiling task as done. docs/METRICS.md's ceiling known-gap is closed.
- **Reason:** Closes the one substantive untried angle on the canonical goal identified this session, and removes a previously-manual, unverified constant (11,644) from the acceptance-criteria table by replacing it with a reproducible computation.

## 2026-09-24: track-c-fuzzy-search-context: close, same negative evidence as Track B plus never in the active goal

- **Context:** track-c-fuzzy-search-context (FuzzyTokenSearcher context-phrase model, two-level fuzzy match pipeline, role tie-breaking for person-name matching) is design-only in PLAN.md's Track C section, which carries doc-status: future -- it predates and was never folded into the segmentation-transfer pivot that is now the sole canonical goal. Separately, S6b/S6c already tested the same class of lever directly against the current goal: fuzzy/dictionary/phrase-based evidence (Group B entity_surface_matches + s4_fuzzy_surface_form_scan, Group C s2_anchor_phrase_inventory + s4_opening_phrase_candidates) fed into segment_gap's position_scores, five variants, all net negative vs. the 0.644 no-evidence baseline (docs/DECISIONS.md 2026-09-21/23). Track C would rebuild the same kind of signal from a different starting package (fuzzy-search by Marijn Koolen) for a goal (person-name/context matching) that doesn't connect to the current pipeline at all per docs/APPROACH_OVERVIEW.md's dependency graph.
- **Decision:** Close track-c-fuzzy-search-context without starting it, same as track-b-name-matching. No further design or implementation work scoped against it.
- **Reason:** User asked directly whether the remaining open tracks were still useful, after Track B closed by judgment call alone; Track C has the same doc-status:future flag as Track B plus direct negative measured evidence Track B never had, so the case to close is stronger, not weaker.

## 2026-09-24: ner-finetuning-original-goal: sync state.json to already-retired status

- **Context:** PLAN.md's canonical-goal block already states this track is retired, not merely deprioritized: GysBERT multi-class NER fine-tuning would train on spans from the same ~50-55%-recall upstream tagger its own training data comes from, so it cannot close its own recall gap (session note 2026-09-18), and its output is unrelated to the current segmentation-transfer goal's work (docs/DECISIONS.md 2026-09-21). state.json still carried this as blocked ('deprioritized while the alignment/segmentation pivot is active'), which undersells how settled this already is and left it resurfacing in track lists.
- **Decision:** Mark ner-finetuning-original-goal done/retired in state.json, matching PLAN.md's own retirement language. Phase 1 data prep (21K LOC + 13K PER training pairs) remains on disk as a byproduct but no Phase 2 (model training) is planned.
- **Reason:** Bookkeeping sync only -- no new evidence, just aligning state.json with a retirement PLAN.md already recorded, per the same rest-of-tracks review that closed track-b and track-c.

## 2026-09-24: hoe-classifier-other-bucket: retire, blocked on missing source data

- **Context:** Kept open 2026-09-24 as an out-of-scope side-track (doc-status:sidelined, not part of the segmentation canonical goal). Checking its actual re-run cost surfaced a real blocker: neither of its two inputs exists on this machine -- the raw source (~/Downloads/annotations-unaggregated/annotations-layer_HOE.tsv.gz) is absent, and the pipeline's own prior output (data/hoe_vocab.parquet, the 1.2M-row/16-category file PLAN.md's numbers come from) is also absent. The only local file with that name (data.bak/hoe_vocab.parquet, 26MB) is from an unrelated, older 6-category scheme (person_profession/person_title/...), not this classifier's output -- useless for judging the real other-bucket's concentration. Benchmarked the pipeline's own fuzzy-fallback step directly (rapidfuzz token_set_ratio, ~1.05M calls/sec): re-processing the full ~574K-distinct-form other bucket would take ~13 minutes single-threaded once the source data is in hand, so compute was never the blocker -- data availability is.
- **Decision:** Retire hoe-classifier-other-bucket rather than leave it open indefinitely. User call after seeing the concrete blocker: not worth tracking as a live side-track when the first step is locating/remounting source data of unknown location, separate from any actual classification work.
- **Reason:** User's explicit instruction after the cost estimate. Recorded so a future session doesn't reopen this expecting the data to already be available, and so the missing-file finding isn't lost if the source ever does need to be revisited.

## 2026-09-25: Step 1 gate PASSED: session order is reliable where the session label is not

- **Context:** plans/COLLISION_AVOIDANCE_TRACK.md Step 1 is the gate for the whole inventory-stream reframing: landmarks must re-impose locality or trading k_e-per-day for k_e-per-inventory is a net loss. Measured by scripts/landmark_density_eval.py (dataset landmark_density_eval, 14/14 tests), thresholds fixed before the run; the day-partition arm reproduces the cited 11,644 ceiling and 535 no-HTR dates exactly and the one-segment arm reproduces finding C's 14,258.
- **Decision:** PASS, and pursue the reframing via content-verified session ORDER rather than via better opening detection. Availability is not the constraint (date region in 100% of 1,690 raw sessions, first region 99.8%, attendance 71.7%); localizability on the paragraph_axis stream reaches 86.0% of 1,316 flat sessions across three complementary channels. The decisive result is reliability: zero non-monotone steps among content-verified landmarks in all five annual inventories, while 80-99% of session labels carry a non-zero raw-vs-flat offset in only 5-11 piecewise runs. Next step is Step 4 (anchor-correction loop), not Step 3.
- **Reason:** The order-preserving verified chain is the scaffold Step 4 said it needed and it already exists on disk, whereas Step 3 still owes a gap/scoring design. Two limits are recorded so the PASS is not over-read: locality genuinely loosens (2.13 dates per segment, median segment k_e 1.33x the day median), and most of the raw +1,329 ceiling gain is the 535 no-HTR dates being absorbed into neighbours -- only +342 survives when they are barred, which is below the 718 Global-stretch gap. So the reframing is usable but does not by itself close Global-stretch at honest locality, and PLAN.md's targets stay unrestated per Step 5.

## 2026-09-25: Step 3 unblocked by a parameter-free formulation; my Step 4-first ranking was readiness, not value

- **Context:** After Step 1 passed I recommended Step 4 ahead of Step 3 because Step 3 'owes a gap/scoring design'. The user pushed back that this is a problem not to sidestep, then proposed edit distance for the entity-sequence alignment. Code check: align_session (build_alignment_new.py:900) aligns the resolution sequence and entities enter only as a per-cell bag (:931); anchor_only_diagonal sends every zero-overlap cell to NO_ANCHOR_DIAG_SCORE (:938-941), which with containment 0.295 is shut most of the time. The overlap tables keep a 4-way match_kind category and no similarity column, and nw_matches_gt_rate is 0.957 correct vs 0.962 false-positive.
- **Decision:** Take Step 3 next, not Step 4. Move the alignment's unit from resolution to entity mention inside Step 1's verified-landmark segments and test it with unit-cost edit distance -- parameter-free, so the 'settle the scoring design first' blocker dissolves rather than being deferred. Keep the traceback; fix gap asymmetry before running; accept transposition over-penalisation as a conservative bias. Graded surface similarity as substitution cost only if that run beats the 0.957/0.962 null. Gate: D1c re-run stratified by IDF band.
- **Reason:** The readiness argument was not a value argument. On value the ranking inverts: Step 3 attacks finding A's dominant loss channel (collisions 4:1 over extent) and finding B's 1,478 directly, whereas Step 1's order-preservation result narrows Step 4's target -- order is already intact among the 71.4% verified, leaving mostly the unverified 28.6%. Hypothesis 5 is also currently untestable rather than untested, which makes the entity-unit change a prerequisite for judging the whole idea. track-b-name-matching (soundex/Levenshtein) was closed 2026-09-23 unmeasured on a prior, so this is not a repeat of a negative result.

## 2026-09-25: External survey read: four design options named, first experiment unchanged

- **Context:** User supplied Prousalis et al., 'A Survey on Sequence Alignment Algorithms and State-of-the-Art Aligners' (ACM CSUR 2025), read in full, and asked whether it changes views or contributes metrics. Its MSA half (star/progressive/iterative/genetic/swarm/HMM/DAC/ML) and its modern-aspects section (quantum, optics) do not apply -- this problem is pairwise. Its benchmarking indicators (reads-aligned %, mapping efficiency, BAliScore, TC-score, sum-of-pairs) do not transfer: they presuppose a reference genome or a curated reference alignment, and the survey itself warns its benchmarks are not comparable across studies.
- **Decision:** Direction unchanged; no metrics adopted from it. Recorded four pairwise-section items in Step 3 of plans/COLLISION_AVOIDANCE_TRACK.md as a post-first-experiment menu, citing primaries (LAST/Kielbasa 2011 adaptive seeds for the dense alphabet; minimap2/Li 2018 concave gap cost for the gap asymmetry; semi-global free end gaps for cross-day spillover; MUMmer/Delcher 1999 MUM+LIS for 4562's non-monotone landmarks) with an explicit guard that the first experiment stays unit-cost and parameter-free. Separately logged a MAPQ-style placement margin under docs/METRICS.md 'Proposed, not yet computed'.
- **Reason:** The survey is a pointer to primary references, not evidence about this corpus, so it changes design options rather than findings -- recorded as such. The guard exists because appending a menu of sophisticated alternatives beside a deliberately parameter-free experiment is exactly how the 'design a scoring scheme before testing the hypothesis' blocker would get rebuilt. Two independent corroborations noted without action: minimap2's seed-chain-extend is the reference implementation of what STEP_S6 designed independently, and the survey's 'banded DP is the bottleneck for long sequences' matches what the S6 Step-1 oracle diagnostic and the S6b anchor-supply harvest each measured here.

## 2026-09-25: Post-mortem code check closed; three carried-forward candidates recorded

- **Context:** plans/POST_MORTEM_CODE_CHECK.md completed all 5 items 2026-09-25 but never wrote the DECISIONS.md entry its own Deliverable section requires, leaving three confirmed findings living only in that document. Found during a limbo sweep of plans/ and state.json.
- **Decision:** Close the post-mortem. Carry forward three candidates, none a correctness bug, none blocking: (1) a data_io.check --freshness extension comparing a dataset's recorded parent_sources/timestamp against its parents' current provenance -- the resolution_concordance staleness that gated the entire Tier O baseline for a week would be equally invisible today; (2) the dead loc_names lookup for places in build_alignment_new.py:342-377, 0% hit rate over 21,184 place references, correct only by accident of the identity fallback; (3) s6b_anchor_harvest.py's output is orphaned -- 589 lines writing a dataset nothing loads -- worth a note in APPROACH_OVERVIEW's orphaned-work section rather than deletion.
- **Reason:** Findings held in a single plan document evaporate when that document closes, which is the same failure mode the post-mortem itself was investigating (the fuzzy-surface-form-scan track's answer existed under a different task id and was never written back). Candidate (1) is the only one with reuse value beyond this repo and is the one worth scoping as real work; (2) and (3) are cleanups that should not be done inline under a read-only audit.

## 2026-09-25: Step 5c.1: the separated set is accurate enough to freeze as a scaffold, except on no-HTR days

- **Context:** Step 5 of plans/COLLISION_AVOIDANCE_TRACK.md proposed treating the 8,015 separated resolutions (68.8% of the 11,644 ceiling) as a frozen scaffold and attacking the gaps around it. Condition 5c.1 blocked that until accuracy was verified, because 'separated' is a coverage criterion (unique start paragraph, extent <=3) that never tests correctness, and Step 2 had shown a trivial uniform spread beating gold's own annotations on it.
- **Decision:** Adopt the scaffold framing, excluding review-code-M (no same-day HTR) days. Measured on 35 boundary-gold days via scripts/separated_accuracy_eval.py: separated placements hit the exact gold start paragraph 62.8% of the time vs 30.6% for non-separated, and 86.9% land within one paragraph vs 62.0%. Production beats the uniform null 2.4-2.7x on correctness (0.628 vs 0.267 separated; 0.503 vs 0.189 overall), answering the doubt Step 2 raised. On gold's segmentable days accuracy is 63.9% exact / 91.1% within one paragraph, median displacement 0. On missing-HTR days it collapses to 27.1% exact with mean displacement 5.87 paragraphs, because axis_for_date falls back to a resolved neighbouring session and places resolutions on borrowed paragraphs.
- **Reason:** Selecting on 'separated' genuinely selects better placements, which is what a scaffold needs; that is a different job from ranking placement rules, which 'separated' still cannot do (uniform 115 vs gold 103 vs production 104 on the 15 fully-located gold days, recomputed under the canonical gold convention). Freezing M-day placements as anchors would be the exact failure mode condition 1 exists to prevent, since a wrong anchor corrupts the gap on each side of it. Sample caveat: the gold sample over-weights hard days (25 of 35 scored days are diff_3plus), so these figures are pessimistic rather than flattering.

## 2026-09-25: Resolution-level alignment: insertions are not a modelled event; encode as a structural constraint, not a penalty

- **Context:** User observation 2026-09-25 while Step 3 (entity-stream edit alignment) was being designed: at the resolution level, deletion is the norm, transposition is rare, and no genuine insertion has ever been observed -- 'in this respect it differs from aDNA procedures', where reads do carry real insertions. The track file had left gap asymmetry as an open 'fix before running' item with no direction attached.
- **Decision:** Forbid the enriched-side gap move outright rather than pricing it, and make the HTR-side 'stay on the same enriched resolution' move zero or near-zero cost -- i.e. a monotone many-to-many (stepwise) alignment, not Levenshtein with reweighted gaps. Concave gap cost is promoted from optional to the natural fit in the prior-art menu. Direction is level-specific: at the entity-mention level HTR-side skips stay cheap (the summary drops detail the HTR keeps); at the resolution level the asymmetry inverts.
- **Reason:** Checked against gold rather than accepted on assertion: boundary_gold_sample's instructions are 'place K_e - 1 cut points over the ordered stream', and its slot vocabulary (kind in cut/start/end_of_last_resolution, plus has_trailing_spillover and starts_mid_resolution) has NO category for an HTR span belonging to no enriched resolution. The ground truth's own design is a surjection of the stream onto exactly K_e contiguous spans, so the no-insertion prior is gold's construction, not merely consistent with it. The trap a bare penalty falls into: K_f > K_e on 10 of the 50 gold days (1626-11-16 is 47 flat records against 14 resolutions), so under a strict one-to-one NW those surplus records MUST take enriched-side gaps -- raising the penalty does not suppress them, it buys them off by forcing wrong substitutions. The surplus is HTR over-segmentation, the mirror of the measured 81.1% under-segmentation; neither is an insertion.

## 2026-09-25: Nihil-actum is a skip constraint on the enriched stream, not a two-sided alignment anchor

- **Context:** User proposed (2026-09-25) using the nihil-actum signal as anchors in Step 6's session-level sequence alignment. Checked both sides against artifacts already on disk before wiring anything in.
- **Decision:** Do not wire nihil-actum in as an anchor class. Use the 127 enriched nihil sittings as a hard a-priori skip constraint (they must consume zero HTR sessions), and harvest the HTR-side nihil/vacat/niet-gedaen marker dates separately as their own landmark inventory.
- **Reason:** The two sides are not the same class. Enriched nihil is a calendar artifact, not a content event: 112 of the 127 dates (88%) are Sundays, against an otherwise even 227-228 per weekday across the enriched date series, and only 2 of the 127 appear in paragraph_axis_1626_1630 at all -- a day with no sitting produced no text, so there is nothing to anchor to. The HTR side carries its own markers on 138 dates (nihil actum 39, vacat 103, niet gedaen 16 paragraphs) with exactly zero date overlap with the enriched set. As a skip constraint the enriched set is valuable precisely because 46.6% of sessions are already mis-keyed by more than one position: 127 guaranteed no-consume positions stop the aligner shifting everything downstream by one. Caveat: the HTR-side 138 comes from crude case-folded substring matching with no orthographic variant sweep, so it is an upper bound; the enriched-side 127 is exact.

## 2026-09-25: Correction: the HTR-side nihil marker set is 32 dates, not 138; the collapse is mostly implicit

- **Context:** A 2026-09-25 entry ('Nihil-actum is a skip constraint...') reported 138 HTR-side marker dates from case-folded substring matching over paragraph_axis_1626_1630, and proposed harvesting them as an independent landmark inventory. User challenged both the marker set and the landmark reading.
- **Decision:** Retract the 138 figure and withdraw the landmark-inventory recommendation. The usable HTR marker set is 32 dates. Keep the enriched-side skip constraint, which the correction strengthens.
- **Reason:** 119 of the 138 were false positives, found by reading the matched text: all 103 'vacat' hits are 'vacatien', per-diem FEES from Latin vacatio in travel-expense declarations, and all 16 'niet gedaen' hits are ordinary prose. Of 46 'nihil' paragraphs only 32 are standalone Nihil Actum markers. The collapse of a nihil sitting into a neighbouring session is mostly IMPLICIT (user): 32 of 127 leave any trace, 95 leave none. That makes the skip constraint essential rather than optional -- with no marker on 75% of cases, an aligner has no local evidence from the HTR side that a sitting is missing and must be told a priori from the enriched side. The 32 that do leave a trace sit at session ends (28 of 32 within two paragraphs of the last) and concentrate one day before the nearest enriched nihil date (17 of 32; enriched 88% Sunday, markers 59% Monday), but direction cannot be settled because the flat session date label is itself the drifted quantity. 32 events is too thin to key an alignment on. Method note: the 138 was reported before the matched text was read.

## 2026-09-25: Step 6's gate: the enriched<->HTR session mapping is already complete wherever there is HTR to map to

- **Context:** plans/COLLISION_AVOIDANCE_TRACK.md Step 6 was ranked ahead of Step 3 on the argument that the enriched->HTR session correspondence is established by date label plus a +/-1-day window, and that 5,619 resolutions (29.4% of 19,120) sit on dates whose session mapping is 'absent or already known to be wrong'. Its own first deliverable was a gate diagnostic measuring the enriched<->HTR session offset across the streams, rather than arguing by analogy from s6b_session_fingerprint_match's flat<->raw offsets (both sides HTR).
- **Decision:** Gate does NOT pass; verdict INCONCLUSIVE against thresholds fixed before the run (determined_share 0.9% vs >=25% required; beyond-window share 100% vs >=50% required). Do not build the session aligner on this evidence. scripts/session_offset_eval.py (+ 13 tests) -> dataset session_offset_eval. The step's factual premise does not hold for the main corpus: in inventories 3185-3189 EVERY enriched sitting already resolves to an HTR session (1,089/1,089) and the concordance has ZERO missing_htr rows there. All 344 unresolved sittings / 4,482 resolutions are in 4562 (2,095) and 4861 (2,387) -- the two non-annual 'secret resolution' series PLAN.md's Local criterion already excludes, where 4861 has no axis sessions at all and 4562 has 102 sessions for 187 sittings. Only 3 sittings (40 resolutions, all 4562) sit in determined brackets. The 1,137 cross_day_shift resolutions on 74 dates are NOT unmapped -- every one carries a resolved session. Two by-products: the no-insertion claim, verified at session level as the design notes required, is nearly but not exactly true (51/1,316 sessions have no enriched sitting; 12 outside 4562); and the enriched edition ends 1630-05-14 while the axis runs to 1630-12-31, so 180 trailing 3189 sessions lie outside the enriched span.
- **Reason:** Step 6's mechanism is confirmed where it applies -- all 3 order-forced pairings are 20/22/36 days from their date label, correspondences the +/-1-day window provably could not reach -- but it applies to 40 of 19,120 resolutions against a Global-stretch gap of 718. Building a content-keyed session aligner to serve 4861 (no HTR) and 4562 (anchor-starved, already excluded from pooling by finding C and carrying Step 1's only 15 non-monotone landmarks) is the expensive path to the least reachable mass in the corpus. NOT tested here, and the honest caveat on this verdict: that a sitting is pinned does not mean it is pinned CORRECTLY, so this closes 'the mapping is absent' as a motivation without settling 'the mapping is wrong'.

## 2026-09-25: The enriched edition ends 1630-05-14; the alignment does not extend beyond it

- **Context:** Confirmed by the user 2026-09-25 after the Step 6 gate diagnostic surfaced that inventory 3189 carries 289 axis sessions against only 108 enriched sittings. Measured: the paragraph_axis_1626_1630 runs to 1630-12-31 and holds 2,284 paragraphs (10.6% of 21,519) over 184 sessions dated after the enriched edition's last sitting -- 180 in 3189, 4 in 4562.
- **Decision:** Treat 1630-05-14 as a hard scope boundary on the HTR side, not just a descriptive fact. scripts/session_offset_eval.py drops post-edition sessions from the stream before bracketing (cut-off derived from the data, not hardcoded) and asserts that no enriched sitting resolves into them; that count is 0 today, so the bar costs nothing now and exists to catch a widened axis_for_date neighbour fallback later. Recorded as a standing guardrail in plans/COLLISION_AVOIDANCE_TRACK.md, because the day partition excluded this material for free by keying paragraph counts on enriched dates while a stream framing does not.
- **Reason:** Enforcing it changed no Step 6 gate quantity (3 forced pairings, 100% beyond the +/-1-day window, 51 no-insertion sessions, same verdict), which is the robustness check; it changes denominators only -- axis sessions 1,316 -> 1,132, unclaimed sessions 263 -> 79, no-insertion 51/1,132 (4.5%), annual inventories 12/1,034 (1.2%). Upstream is already safe and was verified rather than assumed: metrics_local_inventory_ceiling and landmark_density_eval's date_sequence both join paragraph_count on enriched_date, so the 11,644 day ceiling, finding C's 14,258 and Step 1's segment ceilings never included post-edition material (3189's 1,579 paragraphs in finding C's table is exactly its in-scope count). The trap is not hypothetical: 3189's post-edition sessions are trailing and bracket geometry isolated them anyway, but 4562's 4 sit inside ordinary brackets where they could be offered to a sitting dated years earlier -- the regression test forces such a pairing at >900 days. Step 5's pending bound re-derivation is the next thing that would hit this: a naive per-inventory paragraph sum would hand 3189 3,858 paragraphs instead of 1,579.
